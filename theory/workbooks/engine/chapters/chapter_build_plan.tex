% Generated by maint/texbuild/theory_tex.py from theory/workbooks/engine/build_plan.md.
% Edits here are overwritten. Edit the markdown, then run the script again.
\chapter{\texorpdfstring{The build plan: the engine}{The build plan: the engine}}
\label{chap:build-plan}

\textbf{Purpose:} The order the engine's wants in \hyperref[chap:engine-table]{engine\_\allowbreak{}table.md} are built in, and every ruling on them, dated, so every session builds the next item and not whatever looks nearest. Built in order, one item at a time; an item is done when its program runs on the device and its row below says so.

\textbf{Two tables, two concerns (Doug, 23 September):} keep the cell table and the engine table separate; each is optimized against the other only through its interface section, never merged. \textbf{The engine is optimized for no scale:} no size, spacing, order, window or width is ever written into engine/. Each comes from the request or the data. The engine table's scale audit lists what is still in; this has been put in and ripped out repeatedly, and it is dangerous.

\textbf{Scope:} the engine's items of the build plan kept from 22 to 24 September. The item numbers are the plan's own, so a number missing here is another book's: the cell program's items are in the cell book's build\_\allowbreak{}plan.md, and the crystal's growth in the compression book's build\_\allowbreak{}plan.md.

Statuses: proved, measured, built, theory, refuted, not so. “Not built” means nothing of it exists yet.

\section{\texorpdfstring{Done before this plan}{Done before this plan}}

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.2237\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.6006\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.1757\linewidth-2\tabcolsep\relax}}
\toprule
item & what it is & status \\
\midrule
\endhead
record programs & \texttt{keymath\_\allowbreak{}record\_\allowbreak{}imprint} proves every register's width, \texttt{key\_\allowbreak{}schedule\_\allowbreak{}record\_\allowbreak{}lay} lays the register file out, \texttt{cycle\_\allowbreak{}record\_\allowbreak{}run} sweeps one launch & built \\
\bottomrule
\end{longtable}
}

\section{\texorpdfstring{Phase A: the vocabulary, once, used by everything below}{Phase A: the vocabulary, once, used by everything below}}

Doug's rule for everything: truthy and falsy vector magnitudes, so the machine steers itself iteratively. Every verdict is a field, zero false and nonzero true; AND is PRODUCT, OR is SUM, NOT is 1 − |COMPARE(t, 0)|, and a value is gated by PRODUCT(value, t). No thresholds and no host decisions: a pass writes its verdicts as records, and the next pass reads them.

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.1114\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.3771\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.5115\linewidth-2\tabcolsep\relax}}
\toprule
item & what it is & status \\
\midrule
\endhead
A1 & tuple lanes: a lane reads fields from 2 or 3 bodies through an index array (a pair, or a parent and two children) & built: the print swept through a reversed index lands 0 of 81,807 prints away from their body, 550 us; direct CRC-64 still \texttt{590f3a2c386647e3}; host port 0 of 81,807 differ \\
A2 & absolute value and compare (−1, 0 or 1) & built: over K\_\allowbreak{}zy of 81,807 bodies (22,364 below zero, 34,872 at zero, 24,571 above), 258 us; 0 break |K| against K, 0 disagree with the print's nonzero band, 0 differ from the host \\
A3 & records are vector magnitudes: the imprint reports each output's field (offset, bits), so any sweep's records are the next program's input, read with FIELD\_\allowbreak{}SIGNED & built: the print reports seven fields at 0:8 8:8 … 48:8; the pair program, its host proof and A2's check all read the print through the reported fields and hold (self-weight 1,275 on all 81,807, 0 differ from the host) \\
A4 & each member of a lane reads its own array (the bodies, the last sweep's records, the tracker's per-body values such as the climbed lag), so a pass reads its own verdicts and the machine steers itself by sweeping again & built: a pass reads each body and its own print side by side with no index, recomputes the mass band, and writes 1 − |COMPARE| against the print's band: truthy on 81,807 of 81,807, 157 us, 0 differ from the host; two members naming the same array share one device copy \\
\bottomrule
\end{longtable}
}

\section{\texorpdfstring{Phase C: the residual as unit sweeps}{Phase C: the residual as unit sweeps}}

{\footnotesize
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.1072\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.0968\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.2555\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.5406\linewidth-2\tabcolsep\relax}}
\toprule
item & row & what it is & status \\
\midrule
\endhead
C5 & 3 & the binomial kept as [1 1]\textasciicircum{}n unit sweeps with B\_\allowbreak{}narrow shared, proved lane for lane against today's key & built and proved 23 September (\texttt{unit\_\allowbreak{}sweep} module; \texttt{-\allowbreak{}-\allowbreak{}unit-\allowbreak{}sweep}, \texttt{-\allowbreak{}-\allowbreak{}unit-\allowbreak{}sweep-\allowbreak{}prove}; \texttt{Engine\allowbreak{}Residual\allowbreak{}Request.\allowbreak{}unit\_\allowbreak{}sweep}). Centred [1 2 1] steps keep the key's mirror boundary exactly; one block a line, all of an axis's steps in shared memory, only the live limbs worked. 0 lanes differ on 500 frames over 5 samples. 14.0 ms a frame against the key's 29.0 (2×); edges identical. The default is still the key (Waiting on Doug 15) \\
\bottomrule
\end{longtable}
}

\section{\texorpdfstring{Phase D: prove and measure}{Phase D: prove and measure}}

\tablerecord{D1}
\begin{description}
\item[{what it is}] the host port of the record machine (\texttt{cycle.\allowbreak{}c}, anchor\_\allowbreak{}sift's exact integer); every program's prints equal the device's bit for bit
\item[{status}] built and run (\texttt{engine\_\allowbreak{}record\_\allowbreak{}host}): the print 0 of 81,807 differ; A1, A2, A4, the pair, B2's check and prediction and B4's triples all 0 differ from the device
\end{description}

\tablerecord{D2}
\begin{description}
\item[{what it is}] why \texttt{flatten} is slow: 138 to 170 s on 44b6\_\allowbreak{}0113de3b, against 8.5 s for the same 81,807 bodies on the tracker's per-frame path
\item[{status}] cause found 23 September (run on a scratch copy of the set, so \texttt{flattened.\allowbreak{}iapx} did not touch the data): the slab. \texttt{flatten} sizes its residual slab to half the free device memory (10 frames here, about 1.5 GB of residual limbs beside all 100 frames of lanes). With it, every stage runs 10 to 20× slower: codes 17.5 s, contract 44.0 s, label 9.2 s, the residual 25.7 s, 107.0 s in all. With a one-frame slab: codes 1.6 s, contract 2.1 s, label 0.6 s, the residual 3.0 s, 9.0 s in all. \texttt{flattened.\allowbreak{}iapx} is byte-identical either way. The grade (the exact-key contract on each sample's frame 0) is 3.4 s at the big slab and 70 ms at one frame, now timed as its own stage. The code still sizes the slab as before (Waiting on Doug 16)
\end{description}

\section{\texorpdfstring{Measured along the way: what a register tier costs}{Measured along the way: what a register tier costs}}

\texttt{-\allowbreak{}Xptxas -\allowbreak{}v}, sm\_\allowbreak{}86. The record kernel's whole frame is its per-lane arrays (\texttt{file[WIDE]}, \texttt{sign[256]}, the ladder's \texttt{reached[WIDE + 2]}), all in local memory, 0 spills. Local memory is VM the driver reserves for every resident thread: the RTX 3070 holds 46 × 1,536 = 70,656.

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.4322\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.2759\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.2919\linewidth-2\tabcolsep\relax}}
\toprule
kernel & frame a thread & VM reserved \\
\midrule
\endhead
record \texttt{<64>}, after A1 & 816 B & 57.7 MB \\
record \texttt{<256>}, after A1 & 2,352 B & 166 MB \\
linear \texttt{<16>} (HEAD) & 336 B & 23.7 MB \\
linear \texttt{<64>} (HEAD) & 1,104 B & 78 MB \\
linear \texttt{<256>} (HEAD) & 4,176 B & 295 MB \\
\bottomrule
\end{longtable}
}

\begin{itemize}
\item The tiers 16/64/256 came in with \texttt{a84f56b} “initial”, never measured. \texttt{ENGINE\_\allowbreak{}RECORD\_\allowbreak{}LIMBS\_\allowbreak{}MOST 256u} copies that tier; \texttt{CYCLE\_\allowbreak{}RECORD\_\allowbreak{}BLOCKS\_\allowbreak{}MOST 4096ull} is invented. Neither is measured yet.
\item The ladder's scratch \texttt{reached[WIDE + 2]} costs as much as the register file itself.
\item \texttt{key\_\allowbreak{}schedule\_\allowbreak{}record\_\allowbreak{}lay} gives every step its own register for the whole run and never reuses a dead one, so the file grows with every step.
\item The first sweep of a run pays about 15 ms that later sweeps do not (15,221 us, then 550 us).
\end{itemize}

\section{\texorpdfstring{Waiting on Doug}{Waiting on Doug}}

\begin{enumerate}
\item Answered 23 September: the unit sweeps become the default residual (tracker and \texttt{flatten}); the key stays for the proof. Done: \texttt{ENGINE\_\allowbreak{}RESIDUAL\_\allowbreak{}BY\_\allowbreak{}UNIT\_\allowbreak{}SWEEP} is 0, the zeroed default. \texttt{-\allowbreak{}-\allowbreak{}residual-\allowbreak{}key} selects the key, and \texttt{-\allowbreak{}-\allowbreak{}unit-\allowbreak{}sweep-\allowbreak{}prove} runs both and compares them. On 44b6\_\allowbreak{}0113de3b by default: the unit sweeps take 1.41 s over 100 frames and the key 0; edges 38/12, unchanged.
\item Answered 23 September: \texttt{flatten} runs one frame at a time, as the tracker does. Done: \texttt{flatten\_\allowbreak{}set} calls \texttt{engine\_\allowbreak{}residual} a frame at a time (its own key, slab, atoms and device lanes are gone), so it takes the unit sweeps too. On 6bba\_\allowbreak{}48816121 (scratch set): 7.7 s, the residual 1.5 s of it; components proved on 100 of 100 frames, 0 bits lost, 0 faces differ on the graded frame; \texttt{flattened.\allowbreak{}iapx} byte-identical (\texttt{cmp}) to the one the old slab wrote. 19b. Answered 23 September: the one microseconds clock is a static inline in engine\_\allowbreak{}config.h; the base modules' copies and engine.cu's go. Done: \texttt{engine\_\allowbreak{}clock\_\allowbreak{}microseconds} lives in engine\_\allowbreak{}config.h; max\_\allowbreak{}tree, box\_\allowbreak{}history, climb\_\allowbreak{}machine and engine.cu no longer keep their own. 20b. Done 23 September: \texttt{climb\_\allowbreak{}machine\_\allowbreak{}extents(machine, const Climb\allowbreak{}Machine\allowbreak{}Extents\allowbreak{}Request *)}; the review edits, the successor helper and the cast comments rebuilt clean and every proof held (C1 0 of 35,755; the box's three proofs 0; C2 0 of 11,780,208 and 0 of 9,021,600; the unit sweep 100 frames, 0 lanes; edges 38/12).
\item Answered 23 September: \texttt{engine\_\allowbreak{}record\_\allowbreak{}sweep} and its unchecked \texttt{*sweep\_\allowbreak{}microseconds} stay as they are.
\item Answered 23 September: a cast across width or sign carries a one-line \texttt{/\allowbreak{}/\allowbreak{}} comment saying what it widens, narrows or re-signs and why that is safe there. It is the one exception to the no-comments rule. Redundant casts are still removed.
\item Answered 23 September: fold both. The microseconds clocks become \texttt{engine\_\allowbreak{}clock\_\allowbreak{}microseconds}, and the key's successor builder becomes one helper in score\_\allowbreak{}sample.cu.
\item Answered 23 September: reshape \texttt{climb\_\allowbreak{}machine\_\allowbreak{}extents} to take one const request struct.
\item Ruled 23 September: stateful graceful errors are the only acceptable error. Kinds: request, resource (a failed CUDA call or allocation) and logic (unforgivable; the result is withheld). Each request carries \texttt{Engine\allowbreak{}Error *error}, and the engine keeps its own record too (first write wins). The record holds kind, module, site (line), status, execaddr, evacaddr, imagebase and up to 16 return-address frames. Builds emit a .pdb (\texttt{-\allowbreak{}Z7}, \texttt{-\allowbreak{}DEBUG}), so execaddr − imagebase steps straight to the source line. Wired so far, each built and checked on 44b6 (38/0/12, proved on 100 frames, 0 lanes differ):
\end{enumerate}

\begin{itemize}
\item engine\_\allowbreak{}frame\_\allowbreak{}bodies; engine\_\allowbreak{}residual (a BOTH\_\allowbreak{}PROVED lane disagreement is a logic error);
\item max\_\allowbreak{}tree (every entry, slide and overlap included), flatten, decimal\_\allowbreak{}double;
\item unit\_\allowbreak{}sweep, cycle (module 5), keymath (6), key\_\allowbreak{}schedule (7), residual\_\allowbreak{}program (8), grow (9);
\item apxrep (10): its file layer (input, history and bodies read/write) takes an error. \_\allowbreak{}IO errors carry errno; bad contents (header, bounds, CRC, trailing bytes) are logic errors.
\item The engine calls that go through apxrep: iapx head/load/prove, ingest, entropy set/cloud/history read, and bodies read/write. The two set requests carry \texttt{error}, and the others take a trailing \texttt{Engine\allowbreak{}Error *}.
\item engine\_\allowbreak{}entropy\_\allowbreak{}set's keep path and schedule's per-sample skip each use a local error, because those reads may miss. The failure path was checked with a missing sample: module 10, site 178, status 2.
\item compression (11) and tower (12): requests carry error; TOOK on every CUDA call. A malformed offset table or chunk count read from a file is a logic error. A coefficient overflow on lift is a request error; a rebuilt value outside 16 bits on lower is a logic error. entry\_\allowbreak{}iapx\_\allowbreak{}encode/decode take the error. Checked: 44b6 held, and iapx-prove proves 1 of 1.
\item RSNA knee (a peer session) relayed Doug's answers:

\begin{itemize}
\item zip becomes its own module, engine/base/representation/zip/, with the ZIP64 walk moved out of npy.c;
\item dicom becomes engine/base/representation/dicom/;
\item one sample is one SeriesUID, the single */<SeriesUID>/ prefix in the zip;
\item “do not bound or truncate the data”: DICOM header bytes go inside the .iapx as a new section. The .iapx framing is still open with Doug.
\end{itemize}
\item entropy\_\allowbreak{}history (13): windows(extent, error); the project request carries error. Broken parity is a logic error. Checked on 44b6.
\item Doug's rulings on .iapx v2:

\begin{itemize}
\item sections in order of how often they're read (the file is read FIFO): head and geometry, then pixel offsets and stream, then deflated DICOM header bytes (which engine\_\allowbreak{}iapx\_\allowbreak{}load hands back), then the member list;
\item CRC-64 per part, with no zip CRC-32 rejoin.
\end{itemize}
\item Doug accepted the counter-proposal put to him:

\begin{itemize}
\item zarr is untouched; dicom is a source kind reading through the zip module;
\item EngineArrayRead gains an optional EngineSideBytes *side;
\item the zip archive is held across samples, keyed by path and indexed by series prefix;
\item equal slice keys break ties exactly by InstanceNumber, then SOP UID; nothing is refused;
\item no bounding: nothing is refused for rescale, signed pixels or transfer syntax (signed pixels become stored + 2\textasciicircum{}15, pending Doug on the mechanism);
\item flatten takes smooth/background orders per sample, and one set holds every geometry.
\end{itemize}
\item NO ROUNDING (Doug, emphatically). The old zebrafish orders \{2,34,34\}/\{6,96,96\} came from chosen scales and n = 4(σ/v)² rounded to even: a debt, never to be reused.
\item Ruled by Doug:

\begin{itemize}
\item Parity (a): refuse an odd background order per axis (request error). An odd smooth order is held, with an exact half-voxel offset recorded as a residual field. The residual is 2\textasciicircum{}G·B\_\allowbreak{}s x − B\_\allowbreak{}b B\_\allowbreak{}s x, so the centres differ by b/2. The guards to change are residual.cu:24, keymath.cu:118 and unit\_\allowbreak{}sweep.cu:254 (narrow\_\allowbreak{}bits at :259).
\item Count rule: order = period (n = P, both in voxel steps).
\item The per-axis coherence reading on volumes is new engine math, still to build (bench\_\allowbreak{}recover\_\allowbreak{}period is the 1-D model).
\end{itemize}
\item Converter work order:

\begin{enumerate}
\item the zip module (ZIP64 walk moves out of npy.c; module 14);
\item dicom;
\item EngineSideBytes on EngineArrayRead;
\item .iapx v2;
\item one series end to end, then ping RSNA (rsna-knee-abnormality-7d). The data: \textbackslash{}server\textbackslash{}S\textbackslash{}kaggle\_\allowbreak{}project\_\allowbreak{}data\textbackslash{}rsna\_\allowbreak{}knee\textbackslash{}rsna-knee-abnormality-detection.zip (265 GB, 819,640 deflate members, train\_\allowbreak{}series|test\_\allowbreak{}series/<Study>/<Series>/<SOP>.dcm).
\end{enumerate}
\item Done 23 September: the zip module (14), dicom (16) and the engine wiring. Detection is PK + the member folder's first file carrying DICM. A file source maps sample to member, and zip\_\allowbreak{}held\_\allowbreak{}release runs at the end of ingest. Test series 1.2.826…903947 (34×960×960 u16) was ingested and proved from the file, CRC-64 45aea19b28ddd8b9, 38.1\% of raw. An independent Python read agreed on the shape, the one prefix and 34 distinct keys. The key runs opposite to InstanceNumber. The set dir needed the 8.3 short path because set + two UIDs exceeds MAX\_\allowbreak{}PATH 260; the engine's opens need a long-path form (to do).
\item The peer's census: all 819,635 members are Explicit VR LE, 16-bit allocated, one sample per pixel, native. 201,276 slices are signed.
\item Ruled by Doug:

\begin{itemize}
\item signed pixels: sign-extend from HighBit to 16 bits, then add 2\textasciicircum{}15 into the u16 lane. Read BitsStored and HighBit; the .iapx records the offset. A slice whose raw words are not exactly the sign-extended form keeps its raw pixel bytes in the side bytes.
\item Deflate encoder: a full LZ77 + dynamic Huffman module, proved by inflate round-trip.
\end{itemize}
\item Doug named the information crystal \texttt{.\allowbreak{}kcr} (Kolmogorov crystal) on 23 September.

\begin{itemize}
\item “do not write version bridges”: .kcr is the only crystal format. Old .iapx crystals are not read; sets are re-ingested.
\item The crystal's apxrep kind is “KCR\textbackslash{}0”, version 1.
\item Layout, read FIFO:

\begin{enumerate}
\item the head (13 words) with its CRC-64;
\item the offsets and limbs with their CRC-64;
\item the deflated side bytes with a CRC-64 of the inflated bytes;
\item the member tables and names with their CRC-64;
\item EOF.
\end{enumerate}
\item engine\_\allowbreak{}iapx\_\allowbreak{}load takes an optional EngineSideBytes *side (the headers are handed back, and lane\_\allowbreak{}offset rides in it); engine\_\allowbreak{}side\_\allowbreak{}release frees it.
\item Names for the other apx files (flattened, OAPX history, BAPX bodies, IMP key) are still open with Doug. .kfc/.kst/.kgr/.ksd were offered; .ans is for the answer file.
\item The code identifiers still say iapx (engine\_\allowbreak{}iapx\_\allowbreak{}*, iapx\_\allowbreak{}path); rename them to crystal.
\end{itemize}
\item Deflate encoder module built (engine/base/representation/deflate). It does exhaustive LZ77 over the 32 KiB window with one-step lazy matching, package-merge length-limited codes, and picks the smallest of dynamic, fixed and stored. It round-trips 13 inputs through our inflate and zlib, landing within about 1\% of zlib-9 on DICOM (no block splitting yet).
\item Doug on output (23 September), answering “printf in the hot loop”:

\begin{itemize}
\item Port the needed MMgr scribo and laboro code (numeros\_\allowbreak{}scribo, verba\_\allowbreak{}scribo, cellularum\_\allowbreak{}laboro in D:/git\_\allowbreak{}project/repos/owned/public/MMgr/src) into our own simple string module. Do not link MMgr.
\item “zero print in any loop unless we raise our hand, else silently update your functionals returns, which get dumped into each print format and called on demand unless an error, so sorta like verbosity but never trap warn or error.”
\item So: functionals fill result structs; one print format per result renders it through the string module on demand; errors and warnings always print.
\end{itemize}
\item Doug: “we should be able to output iapx as any valid format, we compress it we can decompress it too.” The export runs .iapx → any source format (dicom zip byte-exact from the side bytes, npy, nifti, tiff, zarr...). Build it after v2.
\end{itemize}

Still to wire:

\begin{itemize}
\item in the engine: tower, compression, entropy\_\allowbreak{}history, stack, golden\_\allowbreak{}bands, link\_\allowbreak{}objects, run\_\allowbreak{}cfg, run\_\allowbreak{}log, cfg\_\allowbreak{}json, relate\_\allowbreak{}frames, double\_\allowbreak{}fields, and cycle.c's host run;
\item the entry\_\allowbreak{}* ingest (source read, zarr and its codecs). Cell-specific modules (bodies, score\_\allowbreak{}sample, answer\_\allowbreak{}key, the measure/*) are due to move to cell\_\allowbreak{}tracking first.
\end{itemize}

\begin{enumerate}
\item Ruled 23 September: CRC every schedule, cfg and key at imprint and verify right before each cycle; CRC every disk read and write, with a flush read-back. Size the buffers generously, by experiment, to match ingest and r/w throughput (Doug suspects a hardware queuing “snap io desert”). Not started.
\item Ruled 23 September: \texttt{decimal\_\allowbreak{}double} never rounds. \texttt{decimal\_\allowbreak{}double\_\allowbreak{}expand} gives the exact leading bits of digits × 10\textasciicircum{}ex in an \texttt{Anchor\allowbreak{}Exact\allowbreak{}Integer} candidate, plus a binary exponent e2. It also reports \texttt{fits} (the candidate holds the whole value), \texttt{terminates} (the binary expansion ends; a tenth does not) and \texttt{needed\_\allowbreak{}bits} (the width to grow to). Checked against Python fractions on 22 cases at 128 and 4096 bits: 1e2200 needs 5109 bits, and a 4096-bit build gets its exact top 4096 with fits 0.
\item Ruled 23 September (“split it, go”): the slide and the overlap are engine math and are back in max\_\allowbreak{}tree, wired for errors. The slide's key voxels became generic probe voxels, and the engine returns each recorded level's probe partition. The present/alone tally left the engine for cell\_\allowbreak{}tracking, and a slide count that disagrees with the tree's is now a logic error. The overlap keeps probes (points) and probe links (point pairs) as its only interface. The key-cell grading that goes with this item is the cell program's (the cell book's build plan, item 24).
\item Ruled 23 September, integrity: “the only acceptable crc is a merkle dag”. Every CRC-64 is replaced by BLAKE3 Merkle roots. Doug's answers:
\end{enumerate}

\begin{itemize}
\item root: the full 32 bytes;
\item shape: dimensional, leaf = one x-row of LE u16 lanes (its own width, BLAKE3 chunks it), then y, z, t, sample, set;
\item stored levels: “every floor of the tower”, so every level's nodes are stored, rows included (about 6\% of a knee crystal at 512 columns);
\item scope: “every crc64, we need 100\% idempotent determinism against cosmic flips” (head, side bytes, members, stream, parts, sets, schedule, keys, flattened, history, bodies);
\item the universal root: “literally every file the engine touches plus one on itself” (inputs read, outputs written, and the engine's own binary);
\item placement: “Measure both, report”: the hash section after the head against a footer, write cost and one-plane check cost on the knee set;
\item repair: fail closed and report the path (sample, t, z, y, row); no parity unless Doug asks.
\item Module name (Latin, to avoid colliding with a user's libblake3): “obsignatio aeterna, then you can use the signum signator nota notarius sigillum sigillator and comprobare officiale or variation”. The module is \texttt{obsignatio} (engine/base/representation/obsignatio, ENGINE\_\allowbreak{}MODULE\_\allowbreak{}OBSIGNATIO 17).
\item Built 23 September: the BLAKE3 port. \texttt{obsignatio\_\allowbreak{}signum} (host, any length, extended output) and \texttt{obsignatio\_\allowbreak{}many} (device, many equal-length messages at a stride; chunk kernel, then level kernels pairing nodes with the odd one carried up, which is BLAKE3's left-heavy tree). One \texttt{\_\allowbreak{}\_\allowbreak{}host\_\allowbreak{}\_\allowbreak{} \_\allowbreak{}\_\allowbreak{}device\_\allowbreak{}\_\allowbreak{}} compression core serves both. Modes: hash, keyed, derive-key context and material.
\item Doug: “the hashing function needs a full test/ suite for validation ... we just make it happen and prove it.” Suite at obsignatio/test (run.sh, obsignatio\_\allowbreak{}test.cu, the official BLAKE3 test\_\allowbreak{}vectors.json). All pass, 0 failures: host vectors 210 (131-byte extended and 32-byte, three modes); device vectors 1,260 (offsets 0/1, strides L/L+1, three messages, three modes); device context 1; every length 0..102,400 host against device in three modes, 307,203; every single-bit flip of every vector input, 1,807,226 (each differs from the original, all distinct); determinism 2; fail closed 20 (refusal, error kind and module, output untouched).
\item DAG node format, ruled 23 September: each level is separated by BLAKE3 derive-key, one context string a level, of the form “obsignatio aeterna 2026-09-23 <level>“ (a later format change takes a new date); every node at a level is hashed in keyed mode under that level's derived key. The sample root seals every section: H(head words ‖ lane-DAG root ‖ stored-stream root ‖ side-bytes root ‖ members root), where the stored stream has per-chunk leaves over the compressed limbs so a flip on disk localizes before decoding.
\item Mutation proof, 23 September: five mutant ports (a block's last byte dropped, the host stack merged on the wrong count, the device carry zeroed, no ROOT flag, the chunk counter held at 0) all fail the suite, each where it should. The every-length and flip checks cannot see a core bug shared by host and device; the official vectors catch those.
\item Built 23 September, the lane DAG: \texttt{obsignatio\_\allowbreak{}level\_\allowbreak{}context}/\texttt{obsignatio\_\allowbreak{}level\_\allowbreak{}key} (12 levels: row, plane, volume, lanes, chunk, stream, side, members, sample, set, file, universal) and \texttt{obsignatio\_\allowbreak{}lanes} (rows → planes → volumes → lanes root, each level one \texttt{obsignatio\_\allowbreak{}many} over the level below; nodes laid out rows, planes, volumes, root, \texttt{obsignatio\_\allowbreak{}lanes\_\allowbreak{}nodes} = tzy + tz + t + 1; OBSIGNATIO\_\allowbreak{}EXTENT\_\allowbreak{}AXES 4 in the header). Tests: level keys 16, lanes reference 7 shapes against a host build node by node, lanes locality 9,234 single-lane flips each changing exactly its row, plane, volume and the root, lanes closed 10. All 0 failed.
\item CRC-64 sites to replace (crc.h, crc\_\allowbreak{}key.h): apxrep, engine.cu, tower (key and schedule), entropy\_\allowbreak{}history, flatten, score\_\allowbreak{}sample, the driver's prints. Format-intrinsic checks stay (zip CRC-32, PNG, inflate/zarr).
\item Ruled 23 September: a stored-stream chunk leaf hashes the chunk's exact bits repacked to start at bit 0 (LSB-first, as compression writes them), prefixed by its bit length; leaves are disjoint. The side root covers both: one leaf over the stored deflated bytes and one over the inflated bytes. \texttt{member\_\allowbreak{}crc} is the zip entry's own CRC-32 (a zip field the byte-exact export needs) and stays. apxrep may not reach obsignatio, so engine.cu computes and checks every root and apxrep only frames the node bytes.
\item Doug on tamper protection (the Cython dependency he will have this compiled to): “the redundancy check happens in one swept cycle with every other process simultaneously”. The caveat given: an unkeyed root can be recomputed by whoever can edit the files; tamper resistance needs the universal root anchored (an Ed25519 signature with the public key compiled in, or a published root). Open for when the universal root and the Cython build come; keep every hash level a batched device pass so it can fold into the swept cycle.
\item Doug, 23 September, the flow: “ingest->seal kcr shards until we accumulate the grains to a lattice and the same goes for the output and any scheduling, we coalesce a universal hash for the state of the program, so this works when users input their own data, they seal it themselves by running.” Each .kcr is a shard sealed at ingest; shard roots fold into the set (lattice) root; outputs and schedules seal the same way; one universal root is the program's state. The engine is deterministic, so rerunning the same input on the same engine must give the same universal root (a reproducibility proof). Doug on its origin: a one-bit flip anywhere in the monolith already stops the program (“on ACCIDENT”), because every stage is exact and chained; the seal makes that stop name its place.
\item Built 23 September: \texttt{obsignatio\_\allowbreak{}bits} (chunk leaves: bit-range messages over a limb stream, LSB-first, each hashed as an 8-byte LE bit length then the bits repacked from bit 0; a check kernel refuses malformed offsets before any write; one thread a message runs the shared stack digest). The core reads message bytes through a source (plain bytes or a bit range), and the host \texttt{obsignatio\_\allowbreak{}signum} and the device share \texttt{obsignatio\_\allowbreak{}digest}. Tests: bits reference 32 (boundary lengths 0, 1, 7–9, 31–33, 63–65, a BLAKE3 chunk ±1, two chunks, first message at bit 5; hash and keyed), bits locality 57,216 (every bit of the stream, padding included), bits closed 11. Whole suite 0 failed.
\item Doug: “our specific engine signature gets hashed and stops the program if its different”. The engine hashes its own binary at start and refuses to run on a mismatch. Open: where the expected hash lives (it cannot sit inside the binary it hashes unless that slot is zeroed while hashing; or a sealed manifest beside it, which brings back the anchor question).
\item Doug: “we do compile time injection for the engine hash”. Open detail: the injected value is either the linked binary's hash with the injection slot zeroed (patched in after the link) or the hash of the engine's sources/objects computed by the build as a define.
\item Found 23 September: the tower transform spans all four axes, so one plane cannot be decoded alone. A partial check without a full decode is possible only on the stored bytes (chunk leaves); checking decoded rows costs a full decode, though a mismatch still names its row. Matters for the placement measurement.
\item Stage A (in progress 23 September): the .kcr seal. engine\_\allowbreak{}config.h has EngineSignum (32 bytes) and EngineSeal (roots sample, stream, side, members, side stored, side inflated; lane nodes; chunk leaves); EngineStream.crc and EngineSideSection.bytes\_\allowbreak{}crc are gone; EngineHistory.sample\_\allowbreak{}crc became EngineSignum sample. Stage B: history payload/cloud, bodies, tower key/schedule CRCs.
\item Stage A done 23 September: the .kcr seal built, the suite rerun with 14 levels (2,182,450 cases, 0 failed), tamper 9/9, the theory (\hyperref[chap:obsignatio-seal]{obsignatio\_\allowbreak{}seal.md}) and the table's M13 and A10 written.
\item Placement measured and ruled 23 September: both alike on the knee (25,218,496 bytes each, prove median 76 ms each over 15 alternating runs). Doug: after the head. The at-end placement, ApxrepSealPlace and --seal-at-end are removed.
\item Witness ruled 23 September. Doug: “we ingest seal and then witness pixel:pixel 1:1 once. then we have our hash and we know our input is verified and witnessed.” Ingest witnesses twice (device voxels against the in-memory source, then a second independent source read pixel for pixel on the host); Doug: keep both.
\item EngineSampleRecord.crystal\_\allowbreak{}read added: a file that apxrep refuses structurally now prints as not read, not as “0 differ”. Verified on the rebuilt engine: a re-ingest wrote a byte-identical crystal, prove 76 ms, tamper 0 cases wrong, the old at-end file refused as not read.
\end{itemize}

\begin{enumerate}
\item Ruled 23 September, device-job scheduling (Doug: “just a normal job scheduling function, super common for stuff like this”). Module name \texttt{tessera}. Doug's answers:
\end{enumerate}

\begin{itemize}
\item jobs are packed by memory, several at once while they fit (not one owner);
\item the device share is a request field: the caller names the byte budget, the engine never chooses one (schedule.cu's free/3·2 is a chosen constant to remove, listed in the scale audit);
\item one arbiter per host/device: “daemon or systemd on linux that self tears down/knocks up, single win pid, single docker hook”, to be deployed onto a SAN;
\item clients talk to it over a local socket (named pipe on Windows, Unix domain socket on Linux);
\item measurement: NVML per-process device memory by pid, “with periodic dead sweeps ... otherwise itll leak over time and lock down memory that is never used”;
\item stale tickets “get dumped to lostandfound”;
\item budget: “caller declares, we measure, they overbudget it could be for a reason we ask they can override, they underbudget we grow and report via warn so accounting isnt perturbed”;
\item over-budget is asked at admission, “but dont throw it away, we have a complete precalc tossing would be a waste, it goes into holding before stalling and dumping to lostandfound”.
\item The peer sessions (RSNA, CASMI) use pinned git dependencies; see \hyperref[chap:peer-sessions]{peer\_\allowbreak{}sessions.md}.
\item Over budget is judged against the job's signum's last measured peak: a job is keyed by the BLAKE3 signum of its request (the engine is deterministic); tessera keeps each signum's measured peak; a never-seen job is admitted on its declaration and measured.
\item Times are per job fields (the holding time before lost and found, and the sweep and idle times travel in the requests; the engine chooses none).
\item First build: all of it (“all of it, use websearch”): the daemon and client over the local socket (Windows and Linux), the driver wired through it, the service files (systemd unit, Windows single instance, docker hook), and a tessera/test suite.
\item Queue order: backfill with the head kept (a smaller job goes ahead only if the head's admission is not delayed).
\item Times: “what I want is the shortest time to change of anything to be the root because that is most likely event, in order”. Every deadline (each job's sweep, each held job's holding expiry, the idle teardown) goes into one min-heap keyed by time to change; the daemon waits on its socket with the root's remaining time, handles the root, re-inserts, in order.
\item Lost and found: on a stall the daemon tells the client, and the client writes its precalc there, sealed, beside the ticket, so it can be resumed.
\item Doug, 23 September: “see how some of our ingest operations are a tower themselves, and can get crammed into one prep cycle, that will save an enormous amount of time”. Ingest reads one lattice five times with different operators (tower lift, source lane seal, decode, rebuilt lane seal, voxel compare); fuse them into two sweeps. Measure the ingest's stage times first.
\item Doug, 23 September: “you should fix any defect found”.
\end{itemize}

\begin{enumerate}
\item Ruled 23 September, the engine reorg: engine/base/ (never sees a body: lattice, representation, codecs, the tower, obsignatio, exact arithmetic, scriptura, tessera), engine/nbody/ (defined over n ≥ 1 bodies, n = 1 the base case; no single/ folder), engine/sims/ (programs composing them: run\_\allowbreak{}cfg, cfg, tsv, nbody\_\allowbreak{}program, the oracle). The test: does the module's request carry a body count. The engine README lays out the six steps (Doug: “yep”): 1 ingest (source → lanes), 2 seal (lanes → signa, the witness), 3 lift (lanes ↔ crystal: tower, compression, apxrep), 4 measure (lattice → fields: residual, moments, entropy), 5 partition (fields → bodies: max\_\allowbreak{}tree, grow, bodies), 6 relate (bodies × frames → links). Each file names the step(s) it belongs to. Move by paste then delete; git index untouched.
\end{enumerate}

\begin{itemize}
\item Then: the anchor\_\allowbreak{}sift port. Doug, after the table was done: “have them read our engine, and discuss the move together, did we miss something? if so it gets added to the table and implemented before moving so as to not disrupt or disrupt them as little as possible, they do not know how to use this engine it is foreign to them completely it has evolved”. Messaged “DMA plan reconciliation (2)” [ce24e8] on 23 September with the reading list (engine/README.md, engine\_\allowbreak{}table.md, obsignatio\_\allowbreak{}seal.md, tessera\_\allowbreak{}scheduler.md) and three asks: their use, the gaps, where it lands. Nothing moves into their tree until agreed; every gap goes into the table and is built here first.
\item Reorg done 23 September: 66 modules moved (copy, diff -r, then delete), build scripts and docs repointed, engine/README.md gained the six steps and every file's step(s) (in the README, since files carry no comments beyond SPDX and cast lines). Every defect in the “Found” lines is to be fixed, not just listed.
\item Still to do: stage B (history payload/cloud, bodies, tower key/schedule crc\_\allowbreak{}key.h, flattened, the driver's fingerprint print), then the universal root and its file, the engine self-hash, and the anchor.
\item Found on the way, 23 September: a prove where no sample held still prints “the set's root, sealing every sample's in the order named” over zero samples; the root is only meaningful when every sample held.
\item Found by the CASMI session, 23 September: host cycle.c is compiled at the library's 16-limb width and refuses a 28-limb step that the device accepts (within ENGINE\_\allowbreak{}RECORD\_\allowbreak{}LIMBS\_\allowbreak{}MOST); host and device widths disagree. Report to Doug.
\end{itemize}

\begin{enumerate}
\item Built 23 September: engine/sims (Doug: “engine simulation files, just like in anchor\_\allowbreak{}sift engine”; categories noise-floor, synthetic n-body lattice, period, anchor\_\allowbreak{}sift art). \texttt{bash engine/\allowbreak{}sims/\allowbreak{}run.\allowbreak{}sh <sim>}; shared sim.h (tally, keyed draws, exact rationals via the exact integer) and sim\_\allowbreak{}camera.h (camera law: Binomial(4S,½)−S shot, Binomial(4r²,½)−2r² read, fixed pattern, gain, moving/dividing ellipsoids with truth). Results: nbody\_\allowbreak{}lattice 9/0; noise\_\allowbreak{}floor 11/0 (BM recovers every seeded stream, planes 0–8 random, truth PTC slope 2.0046 vs 2, data-only routes biased 7.0 and 4.5 vs 1 by moving bodies); period\_\allowbreak{}power 599/1 (fails on purpose, see 29); fixed\_\allowbreak{}pattern 10/0; classify\_\allowbreak{}reject\_\allowbreak{}recover 7/0. Facts sent to the TeX writer. The engine has not been run on the sim's lattice.npy yet.
\item Ruled by Doug 23 September (“fix all of them”): both period findings fixed and proved. (a) The period is now the smallest candidate whose height clears the band top (the fundamental), not the strongest peak: from amp 64 up, 32/32 volumes read 5, 0 multiples (was 8/20 at amp 32). (b) The null is a per-axis line shuffle (each line along the axis permuted alone, keyed Fisher–Yates), not a global Feistel: z/y false periods at amp 1024 fell from 23/64 to 5/64 at 8 draws, 2/64 at 19. period\_\allowbreak{}test 700/0 (host reference rewritten to the same rule), period\_\allowbreak{}power 599/0 (the deliberate-fail check now passes). Old global-shuffle and strongest-peak behavior superseded. Timed 23 September (scratch program, RTX 3070, 34×960×960 smooth ramp + noise): period\_\allowbreak{}draw 455.9 ms a call, marginal draw in period\_\allowbreak{}read 450.1 ms, fixed 133.7 ms; 2.96× the global shuffle's 152 ms. At 73,158 draws a series that is \textasciitilde{}9.1 h a series, \textasciitilde{}223,000 GPU-hours for the set (was \textasciitilde{}75,000), so the set-wide band is even more necessary. period\_\allowbreak{}draw allocates per call (not separated). Sent to the TeX writer.
\item Ruled by Doug 23 September (“increase the scheduler steps to n” + “implement the lut”): done. (a) ENGINE\_\allowbreak{}RECORD\_\allowbreak{}STEPS\_\allowbreak{}MAX decoupled from ENGINE\_\allowbreak{}RECORD\_\allowbreak{}LIMBS\_\allowbreak{}MOST, raised to 1024 (the sign array's local-memory cost is the bound; scale audit). (b) key\_\allowbreak{}schedule gained opt-in register reuse (KeyScheduleRecordRequest.reuse / EngineRecordRequest.reuse): liveness free-list frees a register once its last reader has run, so a long chain fits the 256-limb file (a 21-step chain: 3 limbs reused vs 21 kept). Default off → proven programs byte-identical. (c) ENGINE\_\allowbreak{}RECORD\_\allowbreak{}TABLE (op 10): a lookup table — the low index\_\allowbreak{}bits of a register select an entry; the engine's nonlinear edge (kolmogorov\_\allowbreak{}arnold.md). Threaded config→keymath→key\_\allowbreak{}schedule→cycle (device + host). Tables compose by reading one through another (in order — Doug: “collapsed in order onto” the 2D floor). Test test/record\_\allowbreak{}table\_\allowbreak{}test.\{cu,sh\}: 15 checks, 0 failed, using the ordinary ops as oracles (x², |x−30000|+100, composition g∘f, reuse, refusals). engine.h EngineRecordRequest gained tables/table\_\allowbreak{}count/reuse.
\item Port survey (23 September): every anchor\_\allowbreak{}sift engine C/CUDA file present here is code-identical or ours is larger (max\_\allowbreak{}tree, climb\_\allowbreak{}machine). Missing by name: the n\_\allowbreak{}body basin modules (removed 22 September in the v2 split, superseded by the cycle/keymath residual), the sha256 tree (the miner's), and the MATLAB/R/Python ports. Nothing ported back without Doug; the art detector (anchor\_\allowbreak{}sift's periodic\_\allowbreak{}energy) now exists as sim code.
\item Ruled by Doug 23 September (“the tower is its own object, the lut is its own object, if we integrate them both we get a root universal”; chose “bijective edges in stream”): done. TowerEdge \{floor, index\_\allowbreak{}bits ≤ TOWER\_\allowbreak{}EDGE\_\allowbreak{}INDEX\_\allowbreak{}BITS\_\allowbreak{}MOST = 20, forward\} in tower.h; TowerLiftRequest/TowerLowerRequest gained edges/edge\_\allowbreak{}count (callers memset → no edges → the tower as before). An edge permutes the low index\_\allowbreak{}bits of every approximation coefficient at floor k (the region after k halvings; floor == floors is the collapsed floor), high bits pass through, so it is a bijection on the word; tower\_\allowbreak{}lift lays E0,L0,E1,…,L(N-1),EN and tower\_\allowbreak{}lower undoes them as EN⁻¹,L(N-1)⁻¹,…,L0⁻¹,E0⁻¹ (a floor's edges unwind as a stack). Validated up front (a permutation, width 1..20, floor ≤ floors), refused before any device work. test/tower\_\allowbreak{}edge\_\allowbreak{}test.\{cu,sh\}: 20/0. Sim engine/sims/root\_\allowbreak{}universal: 1268/0. Exact through lift→code→wipe→decode→lower on 48 camera volumes (4×12×48×48, 3×13×37×41, 171 edges incl. 20-bit). Fold 8/8: a,b on one floor = b(a); b,a differs; a floor between them blocks the fold. Price (coded bits/voxel, plain 6.802): floor 0 at widths 2/4/8/12 → 6.810/6.908/8.719/12.608; middle floor (72 coefficients) ≤ 6.817; collapsed floor 6.802 (free). So an unfitted edge is exact but costs the magnitude coder unless it sits high in the tower or is fitted, which feeds the open “which functions to tabulate”. Engine rebuilt and published. Not yet: the edges are not wired into engine.cu's crystal/seal path (the .kcr would have to record the edge program) — Doug's call.
\item Doug 23 September (“reaching a boundary here means we need a dimensional expansion to realize the transform”; approved): the Bennett embedding (x, y) → (x, y ⊕ f(x)) makes any f a permutation of the widened field, so a lossy table rides the tower's reversible stream at the price of width (A14's “only permutations” bound becomes a width statement). No engine change; tables built on the host. tower\_\allowbreak{}edge\_\allowbreak{}test now 25/0: |x| and compare as 16-bit Bennett edges round-trip on 64 lattices, f(x) reads out of the carrier on the collapsed floor, the edge touches only its coefficient, mid-tower Bennett edges round-trip, |x| laid bare is refused. Naimark/Stinespring/purification are the quantum forms of the same expansion, which generally bring square roots.
\item Doug 23 September (terms approved: ask, state, answer distribution, magnitude, phase, complete ask, valid set, crossing rule; “represent square roots as their real number with their square operation”; “why would you use 64 bit?”): sim engine/sims/ask\_\allowbreak{}state, 33/0. Exact rationals on the exact integer (word-gcd reduced when they fit, exact unreduced otherwise), Q(√3) numbers as rational + root·√3 with (√3)² = 3 and exact signs. The rational tetrahedral ask (|v|² = 3/4, r = 4Σpv) and the true SIC (|v|² = 1, r = 3Σpv): 25 states (21 pure, 4 mixed) return exactly; pure on the boundary, mixed inside; crossings 75/75; the phase: 14 equator states all ½,½ on the 0/1 ask, all 91 pairs told apart; the weights onto +x are 3/2, −1/2 and ½ ± ½√3 (negative, not classical); (1,0,0,0) refused, crosses to 3/2 and ½ + ½√3; grid 455 at denominator 12: 55 and 87 states, 104 and 72 non-states that still cross to probabilities on x, y, z; the two asks cross into each other 25/25. Host exact arithmetic, no GPU work. Next on Doug's word: KA ψ with symbolic exponents (Doug: “the exp is symbolic after the second nth exp”; a sparse (digit, exponent) form, costs polynomial in the scale); the tower\_\allowbreak{}lower hat-function limit; camera-parameter recovery sim.
\item Doug 23 September (“Yes” to the ψ sim; “the exp is symbolic”; “our boundary is truthy falsy ... with mpprec”): sim engine/sims/ka\_\allowbreak{}psi, 114/0, 28 s. Values at depth are sparse sums c·γ\textasciicircum{}−e with e = β(L) held as an integer and never expanded. The exact sign comes from the head plus a proven tail bound, decided by bit lengths when the gap is wide. Results:

\begin{itemize}
\item Sprecher (2.5) is exact: 2207/4000 > 11/20. It descends on 10 of the 99,999 pairs of D\_\allowbreak{}5, the first at 0.09000.
\item Köppen, both readings of the carried midpoint: (2.9) adds (γ−2)/2 units (symmetric) and (2.7) as printed adds (γ−1)/2. For n = 2 and n = 3 at γ = 10, on every point of D\_\allowbreak{}1..D\_\allowbreak{}5: the pair step equals the level recursion, ψ is strictly increasing, the least gap is exactly γ\textasciicircum{}−β(L), and the widest gap equals the recursion G\_\allowbreak{}L = ½G\_\allowbreak{}\{L−1\} − (s/2)u\_\allowbreak{}L, with s = γ−2 for (2.9) and γ−3 for (2.7).
\item The chained step, checked symbolically to L = 60 (β = 2\textasciicircum{}60−1) and L = 38 (β ≈ 6.75e17): order kept (u\_\allowbreak{}\{L−1\} > (γ−1)u\_\allowbreak{}L), least gap kept (u\_\allowbreak{}\{L−1\} > (γ+1)u\_\allowbreak{}L), widest gap > least, ≤ 2\textasciicircum{}−(L−1)/γ (continuity) and within Lemma 2.3. The symbolic G\_\allowbreak{}L equals the measured one on D\_\allowbreak{}2..D\_\allowbreak{}5. A keyed depth-L point's gap lies between the least and the widest.
\item Both readings give a continuous, strictly increasing ψ; the discrepancy between (2.7) and (2.9) changes only the widest gap.
\item Separation (Doug “Ready go”), now 152/0. ξ = Σα\_\allowbreak{}pψ(x\_\allowbreak{}p) was computed exactly on D\_\allowbreak{}k\textasciicircum{}n (n = 2: k ≤ 3, 10\textasciicircum{}6 points; n = 3: k ≤ 2), with the α tails bounded.

\begin{itemize}
\item LEMMA 3.4 IS FALSE AT k = 1. ξ(0.1, 0) − ξ(0, 0.9) = 1/10 − (9/10)α₂ = 0.0090999… < γ\textasciicircum{}−2, and the n = 3 analogue gives 9.0998 units. Lemma 3.8's supports (ramp γ\textasciicircum{}−β(k+1)) overlap at k = 1.
\item The proof's break: (3.11), the α cut at r ≤ k, does hold. It measures exactly 10, 50 and 500050 units for n = 2 at k = 1, 2, 3, and 10 and 5000 units for n = 3 at k = 1, 2. But “the tails cannot annihilate it” does not mean “the tails keep it at or above the bound”.
\item Correct: |µ\_\allowbreak{}k| ≥ γ\textasciicircum{}−nβ(k) − Σ\_\allowbreak{}\{p≥2\}ε\_\allowbreak{}\{k,p\}, about (γ−1)u' with u' = γ\textasciicircum{}−β(k+1). That beats the image width (γ−2)b\_\allowbreak{}k ≈ 8.81u' (n = 2) and 8.88u' (n = 3). With ramp ρ = γ\textasciicircum{}−(β(k+1)+2), the supports are disjoint at every k: checked symbolically for k = 1..57 (n = 2) and k = 1..35 (n = 3). The paper's ramp is covered at no k by this bound.
\item Measured from k = 2 the true gaps are far wider (49.5, 500049.5 and 5000 units). The shifts check: m + 1 = 2n + 1 shifts never share a gap; with m + 1 = γ they do.
\end{itemize}
\item Doug (“You have division”, “2s compliment”, “Mulmask exact”, “add that to the exact arithmetic”): engine/base/no\_\allowbreak{}rounding/exact\_\allowbreak{}integer gained three functions, and test/exact\_\allowbreak{}divide\_\allowbreak{}test passes 8/0.

\begin{itemize}
\item anchor\_\allowbreak{}exact\_\allowbreak{}divide: Knuth D in base 2\textasciicircum{}32. It returns the quotient toward zero and a remainder with the numerator's sign. The add-back branch was confirmed to run, using an instrumented scratch copy.
\item anchor\_\allowbreak{}exact\_\allowbreak{}divide\_\allowbreak{}exact: the mulmask exact division. Twos shift out; the result is the odd part's inverse mod 2\textasciicircum{}(32·limbs), by Newton x(2 − dx) with a two's-complement negate, then a multiply and a mask. It is multiplied back, and a remainder is refused.
\item anchor\_\allowbreak{}exact\_\allowbreak{}gcd: Stein's binary gcd.
\item New statuses ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}BY\_\allowbreak{}ZERO and ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}NOT\_\allowbreak{}EXACT.
\item sim\_\allowbreak{}rational.h now keeps every value in lowest terms at any width. ka\_\allowbreak{}psi (152/0) and ask\_\allowbreak{}state (33/0) are unchanged in their verdicts and now print exact fractions (2207/4000, 11/20).
\item Then Doug: Schönhage–Strassen, “We use karatsuba”, “Expand the header to accept any n bits power of 2”, “Static assert”.

\begin{itemize}
\item The header takes ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}BITS or ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}LIMBS: any power of two ≥ 32 bits, no ceiling. The limbs/bits pair is derived and must agree; DIGITS was briefly min(1024, what fits) and is back to anchor\_\allowbreak{}sift's fixed 1024 floor (see the integration below). All checked by static asserts, and the ladder rungs are static-asserted too.
\item Width-sized working copies are compile-time rooms: stack up to ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}STACK\_\allowbreak{}LIMBS = 4096, heap beyond, WILL\_\allowbreak{}NOT\_\allowbreak{}FIT if not held.
\item Ladder: long multiplication < ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}KARATSUBA\_\allowbreak{}LIMBS = 32, then Karatsuba, then the SSA transform (ring 2\textasciicircum{}n+1, shifts as twiddles, ψ weighting for the negacyclic product, depth by an integer cost model) from ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}TRANSFORM\_\allowbreak{}LIMBS = 8192 (the measured crossover; the transform is 1.44× ahead at 16K, 1.55× at 32K, 1.85× at 65K).
\item gcd is now Lehmer (Knuth L) and returns a status. The exact division's Newton inverse doubles its precision and runs on the ladder.
\item test/exact\_\allowbreak{}transform\_\allowbreak{}test passes 3/0 at 128 limbs, 4096 limbs and 4,194,304 bits (operands to 65,536 limbs).
\item exact\_\allowbreak{}divide\_\allowbreak{}test passes 9/0 (it adds gcd = Euclid on the long division). ka\_\allowbreak{}psi passes 152/0 and ask\_\allowbreak{}state 33/0.
\item Newton division BUILT (Doug “Add the newton port”): a half-precision recursive reciprocal, one Newton step, exact correction. ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}NEWTON\_\allowbreak{}LIMBS = 8192, measured 2n/n on the Karatsuba ladder: Knuth leads to 4096; Newton is 1.21× at 8192, 1.06× at 16384, 1.81× at 32768 and 2.44× at 65536. exact\_\allowbreak{}transform\_\allowbreak{}test passes 4/0 at 128 and 4096 limbs.
\end{itemize}
\item Doug (“write these exact limb functions as computer key math primitives ... projected into a bit array of the same dimensions”): record ops ENGINE\_\allowbreak{}RECORD\_\allowbreak{}QUOTIENT = 11, REMAINDER = 12, GCD = 13, EXACT\_\allowbreak{}QUOTIENT = 14.

\begin{itemize}
\item Wired through engine\_\allowbreak{}config.h, keymath imprint (bits: quotient = left, remainder = min, gcd = max), key\_\allowbreak{}schedule refs, cycle.c (host, the library itself) and cycle.cu (device Knuth D, Euclid, and multiply-and-mask on the register arrays).
\item The kernel is templated <WIDE, DIVIDES>, so only a dividing program carries 5·WIDE+4 limbs of scratch.
\item test/record\_\allowbreak{}divide\_\allowbreak{}test passes 15/0: device = host word for word in the 64- and 256-limb files, with identities, exact factors, refusals and imprint order. record\_\allowbreak{}table\_\allowbreak{}test still passes.
\end{itemize}
\item Doug (“integrate anchor\_\allowbreak{}sift's engine so we can copy push ours to anchor\_\allowbreak{}sift”): THIS TREE IS NOW THE SOURCE.

\begin{itemize}
\item build\_\allowbreak{}engine.sh and build\_\allowbreak{}driver.sh default ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}ROOT to \$TOP/engine/base/no\_\allowbreak{}rounding.
\item 18 files (no\_\allowbreak{}rounding arms, nbody/anchor\_\allowbreak{}sift, render) were code-identical to anchor\_\allowbreak{}sift's; ours had lost doc text to a sloppy prose pass, so anchor\_\allowbreak{}sift's versions were copied in.
\item exact\_\allowbreak{}integer.h/.c were merged by hand: our code with anchor\_\allowbreak{}sift's contract, and stale notes fixed. ANCHOR\_\allowbreak{}EXACT\_\allowbreak{}DIGITS is back to the fixed 1024 floor (a narrow width must declare it; checked 1..64 limbs, and undeclared at 8 limbs is refused by name). check\_\allowbreak{}exact\_\allowbreak{}limbs.py's regexes still find the literal \#define lines.
\item The engine DLL builds. Nothing has been written into anchor\_\allowbreak{}sift's repository yet. A peer session “engine git dependency integration” exists and may overlap.
\end{itemize}
\item Doug (“Compute chaitins constant”): sim engine/sims/chaitin\_\allowbreak{}omega, Omega for the BLC normalization machine (a closed term halts when it has a normal form).

\begin{itemize}
\item Closed terms are unranked from exact counts (checked against a parse of every code up to 22 bits) and run by normal order on 16 threads, with Brent watching for loops.
\item The tail past L is counted in directed fixed point: parse mass 1 = the smaller root of T² − 3T + 2. Normal forms past L add to the lower bound.
\item L = 30: 0.122543 ≤ Ω ≤ 0.125993, 2 bits. (0.12597 was a hand-conversion error, caught by the writer.)
\item Doug (“everything is truthy falsy steer”, “loop with no answer after information space has been searched is an exit condition”): the exits are now halts / loops / grows forever / out of steps / outgrew the space.

\begin{itemize}
\item Bounded space ⇒ halt or repeat (pigeonhole): at 1e6 steps, “out of steps” was 0 through 30 bits.
\item Growth proof (omega\_\allowbreak{}grows\_\allowbreak{}forever): a spine part S, reduced only by head steps at spine position ≥ its own, returns deeper on the spine as S B⃗ ⇒ no head normal form ⇒ no normal form. It closes 213 of the 587 unresolved terms through 30 bits (200k steps, 16384 tokens). 64× more space moved none of the rest into halts or loops. The BB table (most steps; BB lambda = the largest normal form) matches BusyBeaverWiki through 22. 11/0.
\end{itemize}
\item L = 40 (283,817,255 terms; 488,151 loops, 474,630 open): 0.12407 ≤ Ω ≤ 0.12599, bit 3 NOT settled. The lower bound needs +0.00093 to pass 1/8, about L ≈ 48, roughly 250× the work: a GPU port or a larger counted halting class. Published: Calude–Dinneen 43/40 bits on their register machine; Tromp's own Ω\_\allowbreak{}λ = .0001… (a different machine); a cryptid at 74 bits.
\item GPU PORT BUILT (Doug “Do it”): omega\_\allowbreak{}device / omega\_\allowbreak{}kernel in chaitin\_\allowbreak{}omega.cu, one term a thread, the host's decisions in the same order; device budgets ≤ 256/256 (steer: anything reaching them, or outgrowing the 2×tokens room, is run by the host under the full budgets, exact since budgets only stop a run). Host handback settled while the next launch runs. Host cross-check through 30 bits (fates + BB + champions) passes; \texttt{cpu} arg = host only.

\begin{itemize}
\item Speed: budgets barely matter for Ω (36 bits: 256/256 vs 2048/2048 exact per writer: lower −3.19e-9, open +2.50e-8, upper +2.18e-8 ≈ 2\textasciicircum{}-25.5; gap 0.00240 ≈ 2\textasciicircum{}-8.7; the earlier “2\textasciicircum{}-22” was a misread)
\item LEDGER BUG (writer reproduced): a line cut inside field 16 then ended by the writer's '\textbackslash{}n' parses whole and wins as first match → wrong champion past 30 (fates/bracket safe). Fix pending Doug's go (he said Stop): end marker/checksum, or drop lines the writer had to end. Doug wants the lambda Ω run BY THE ENGINE (record machine), no step/token/width limits; awaiting his go., so Ω runs use 256 256. RTX 3070, 384 threads/SM (cache-bound; more is slower): 44 bits 3.39G terms in 28 s (121M/s, \textasciitilde{}7.5× the host at 256/256).
\item L = 44 (256/256): 0.124438573589 ≤ Ω ≤ 0.125987962504; lower short of 1/8 by 0.000561; first typed halt (1). Upper barely moves with L, so bit 3 settles only from below (needs Ω ≥ 1/8); lower's per-bit gain shrinks \textasciitilde{}0.93/bit ⇒ crossing ≈ L 54 (\textasciitilde{}3.7 h device) if the trend holds.
\item BB check split: “≤ published, = where settled” always; strict equality only at budgets ≥ 2048/2048.
\item JOBS (Doug: “why aren't you planning these as jobs for the engine and chunking”): \texttt{-\allowbreak{}-\allowbreak{}ledger <path>} plans each length in 2\textasciicircum{}28-term jobs; each finished job's exact tally is one flushed line; reruns keep whole lines under the same budgets and run the rest (resume; longer L costs only new lengths). Cut lines are ended before appending; a line must parse whole and its fates sum to its count. Tested: fresh 31 → extend 33 (2 run, 27 kept) → rerun (29 kept) identical; cut last line → 1 rerun, identical.
\item RUNNING: \texttt{omega\_\allowbreak{}jobs.\allowbreak{}exe 54 256 256 -\allowbreak{}-\allowbreak{}ledger build/\allowbreak{}omega\_\allowbreak{}ledger.\allowbreak{}tsv} (\textasciitilde{}3.7 h est.). Read a shorter L's bracket from the ledger any time by running that L with the same ledger (all kept).
\end{itemize}
\end{itemize}
\end{itemize}
\item Next: the outer-function contraction (Theorem 3.3) with the narrow ramp; ψ at the shifted points x + qa (non-grid, a = 0.0qqq… in base γ).
\end{itemize}
\end{enumerate}

\begin{itemize}
\item Found on the way: a series name matching no DICOM member falls through to the npy reader (error module 15) instead of a “no such member” refusal; a failed ingest leaves the empty sample directory it made (engine\_\allowbreak{}directories\_\allowbreak{}make must report what it made, and its walk[1024] is a chosen bound, with the long-path work).
\end{itemize}

\begin{enumerate}
\item Tessera daemon, 24 September. Built and proved; moved to engine/daemon (Doug: “Put it in engine/daemon”); README on starting it and submitting jobs. Seal done (Doug: “Go”):

\begin{itemize}
\item The history is records plus an obsignatio\_\allowbreak{}seal (host keyed BLAKE3, file level). A load whose length breaks, that has no seal, or whose seal fails is refused and the daemon exits.
\item Failed saves are reported on stderr.
\item Tickets end with \texttt{seal <hex>}; the precalc note needs a seal that holds and seals the ticket again.
\item The job test runs in a scratch TESSERA\_\allowbreak{}STATE: 24/0, including damaged, cut and unsealed histories refused and the kept peak surviving a restart.
\item The old unsealed real history was moved to \%LOCALAPPDATA\%\textbackslash{}tessera<uuid>\textbackslash{}history.unsealed.
\item The history now loads before the pipe or socket exists, and the note uses s\_\allowbreak{}daemon\_\allowbreak{}precalc. Rerun at 24/0.
\item Doug: “The engines own runs need to go through tessera”. The driver's jobs are the cell program's (the cell book's build plan, item 36).
\item Sims submit (24 September), each 0 failed with its tessera checks:

\begin{itemize}
\item sim\_\allowbreak{}job.cu with sim\_\allowbreak{}job\_\allowbreak{}submit/sim\_\allowbreak{}job\_\allowbreak{}release; sim\_\allowbreak{}close releases; run.sh builds the client and the daemon beside the sim.
\item Declared and peak: period\_\allowbreak{}power 262,144/145,915,904; nbody 26,542,080/173,178,880; noise\_\allowbreak{}floor 9,142,272/187,858,944; root\_\allowbreak{}universal 339,510/152,207,360; fixed\_\allowbreak{}pattern 141,056/143,818,752; classify 118,016/145,915,904.
\item Ω now builds (the capture fix was in) and runs on the engine at L 16 as a job: 16/0, peak 286,425,088.
\item Nothing loads the DLL. cell\_\allowbreak{}shift is anchor\_\allowbreak{}sift's engine.
\end{itemize}
\item Linux run (24 September, WSL 2, gcc 13.3, CUDA 13.3). Transport: a tree from a scratch index, then git archive piped to \textasciitilde{}/tessera\_\allowbreak{}linux; /mnt is banned by the hook.

\begin{itemize}
\item Fixed: PATH\_\allowbreak{}MAX via linux/limits.h; \_\allowbreak{}GNU\_\allowbreak{}SOURCE in the measure; the timer's return; ENGINE\_\allowbreak{}NOINLINE\_\allowbreak{}HELPER; platform strings guarded; the daemon uses its string table, and the ticket names its signum.
\item WSL's NVML lists no process's memory, so tessera\_\allowbreak{}measure\_\allowbreak{}paravirtual refuses and the daemon exits. The suite passes with 0 warnings.
\item Native Linux is not available here.
\end{itemize}
\item The crystal's growth between 22 and 24 September is the compression book's (its build plan).
\item Service files (24 September): engine/daemon/service/tessera@.socket and tessera@.service (systemd user units). Activation was proven in WSL: systemd started the daemon on a client's connection, then the WSL measure refusal ended it. Windows needs no service. The docker mount is documented, not run.
\item Reservation rule done (24 September, by the stop hook's “finish all open items”): tessera\_\allowbreak{}ledger\_\allowbreak{}wants = max(declared, kept peak) is used for admission, the shadow, the spare and the reservation at start. The ledger budget test is 22/0; the job test is 24/0. The real prove reserved 3,958,566,912, its kept peak.
\item Docker mount run (24 September): Docker Engine 29.1.3 in WSL; the static test/tessera\_\allowbreak{}socket\_\allowbreak{}probe.c in a container, socket mounted, TESSERA\_\allowbreak{}RUNTIME=/run/tessera. systemd started the daemon on the container's connection; the WSL refusal ended it.
\item Open: native Linux (no native NVIDIA Linux driver on this machine); the is not run; no service files; engine runs don't submit through tessera (M14); the Ω engine-run path (lambda capture at about chaitin\_\allowbreak{}omega.cu:2788) is unbuilt.
\item WSL self-report (24 September, Doug: “Use wsl”): under WSL each client reports its own device bytes (dxcore, \texttt{tessera\_\allowbreak{}self.\allowbreak{}c}, TESSERA\_\allowbreak{}ASK\_\allowbreak{}MEASURED); the WSL job test is 24/0. The systemd job test failed on its first pass because a daemon left over from the suite unlinked systemd's socket file at idle. Fixed in tessera\_\allowbreak{}daemon.c: a daemon probes the path before binding and refuses if something answers, and it unlinks only the file it bound (dev/inode). After the fix: 3 of 3 passes 24/0, the Windows suite passes, and a container's job was admitted through the mounted socket.
\item Reservation rule: Doug, 24 September (relayed by the writer, then held): “We reserve what they ask for and then if it cost less we remember that for next time”. tessera\_\allowbreak{}ledger\_\allowbreak{}wants is still in the working tree, waiting on his clarification. Not reverted.
\end{itemize}
\end{enumerate}

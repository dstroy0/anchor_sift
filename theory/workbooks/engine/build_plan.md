# The build plan: the engine

**Purpose:** The order the engine's wants in [engine_table.md](engine_table.md) are built in, and every ruling on them, dated, so every session builds the next item and not whatever looks nearest. Built in order, one item at a time; an item is done when its program runs on the device and its row below says so.
**Two tables, two concerns (Doug, 23 September):** keep the cell table and the engine table separate; each is optimized against the other only through its interface section, never merged. **The engine is optimized for no scale:** no size, spacing, order, window or width is ever written into engine/. Each comes from the request or the data. The engine table's scale audit lists what is still in; this has been put in and ripped out repeatedly, and it is dangerous.
**Scope:** the engine's items of the build plan kept from 22 to 24 September. The item numbers are the plan's own, so a number missing here is another book's: the cell program's items are in the cell book's [build_plan.md](../cell_tracking/build_plan.md), and the crystal's growth in the compression book's [build_plan.md](../compression/build_plan.md).

Statuses: proved, measured, built, theory, refuted, not so. "Not built" means nothing of it exists yet.

## Done before this plan

| item | what it is | status |
|---|---|---|
| record programs | `keymath_record_imprint` proves every register's width, `key_schedule_record_lay` lays the register file out, `cycle_record_run` sweeps one launch | built |

## Phase A: the vocabulary, once, used by everything below

Doug's rule for everything: truthy and falsy vector magnitudes, so the machine steers itself iteratively. Every verdict is a field, zero false and nonzero true; AND is PRODUCT, OR is SUM, NOT is 1 − |COMPARE(t, 0)|, and a value is gated by PRODUCT(value, t). No thresholds and no host decisions: a pass writes its verdicts as records, and the next pass reads them.

| item | what it is | status |
|---|---|---|
| A1 | tuple lanes: a lane reads fields from 2 or 3 bodies through an index array (a pair, or a parent and two children) | built: the print swept through a reversed index lands 0 of 81,807 prints away from their body, 550 us; direct CRC-64 still `590f3a2c386647e3`; host port 0 of 81,807 differ |
| A2 | absolute value and compare (−1, 0 or 1) | built: over K_zy of 81,807 bodies (22,364 below zero, 34,872 at zero, 24,571 above), 258 us; 0 break \|K\| against K, 0 disagree with the print's nonzero band, 0 differ from the host |
| A3 | records are vector magnitudes: the imprint reports each output's field (offset, bits), so any sweep's records are the next program's input, read with FIELD_SIGNED | built: the print reports seven fields at 0:8 8:8 … 48:8; the pair program, its host proof and A2's check all read the print through the reported fields and hold (self-weight 1,275 on all 81,807, 0 differ from the host) |
| A4 | each member of a lane reads its own array (the bodies, the last sweep's records, the tracker's per-body values such as the climbed lag), so a pass reads its own verdicts and the machine steers itself by sweeping again | built: a pass reads each body and its own print side by side with no index, recomputes the mass band, and writes 1 − \|COMPARE\| against the print's band: truthy on 81,807 of 81,807, 157 us, 0 differ from the host; two members naming the same array share one device copy |

## Phase C: the residual as unit sweeps

| item | row | what it is | status |
|---|---|---|---|
| C5 | 3 | the binomial kept as [1 1]^n unit sweeps with B_narrow shared, proved lane for lane against today's key | built and proved 23 September (`unit_sweep` module; `--unit-sweep`, `--unit-sweep-prove`; `EngineResidualRequest.unit_sweep`). Centred [1 2 1] steps keep the key's mirror boundary exactly; one block a line, all of an axis's steps in shared memory, only the live limbs worked. 0 lanes differ on 500 frames over 5 samples. 14.0 ms a frame against the key's 29.0 (2×); edges identical. The default is still the key (Waiting on Doug 15) |

## Phase D: prove and measure

| item | what it is | status |
|---|---|---|
| D1 | the host port of the record machine (`cycle.c`, anchor_sift's exact integer); every program's prints equal the device's bit for bit | built and run (`engine_record_host`): the print 0 of 81,807 differ; A1, A2, A4, the pair, B2's check and prediction and B4's triples all 0 differ from the device |
| D2 | why `flatten` is slow: 138 to 170 s on 44b6_0113de3b, against 8.5 s for the same 81,807 bodies on the tracker's per-frame path | cause found 23 September (run on a scratch copy of the set, so `flattened.iapx` did not touch the data): the slab. `flatten` sizes its residual slab to half the free device memory (10 frames here, about 1.5 GB of residual limbs beside all 100 frames of lanes). With it, every stage runs 10 to 20× slower: codes 17.5 s, contract 44.0 s, label 9.2 s, the residual 25.7 s, 107.0 s in all. With a one-frame slab: codes 1.6 s, contract 2.1 s, label 0.6 s, the residual 3.0 s, 9.0 s in all. `flattened.iapx` is byte-identical either way. The grade (the exact-key contract on each sample's frame 0) is 3.4 s at the big slab and 70 ms at one frame, now timed as its own stage. The code still sizes the slab as before (Waiting on Doug 16) |

## Measured along the way: what a register tier costs

`-Xptxas -v`, sm_86. The record kernel's whole frame is its per-lane arrays (`file[WIDE]`, `sign[256]`, the ladder's `reached[WIDE + 2]`), all in local memory, 0 spills. Local memory is VM the driver reserves for every resident thread: the RTX 3070 holds 46 × 1,536 = 70,656.

| kernel | frame a thread | VM reserved |
|---|---|---|
| record `<64>`, after A1 | 816 B | 57.7 MB |
| record `<256>`, after A1 | 2,352 B | 166 MB |
| linear `<16>` (HEAD) | 336 B | 23.7 MB |
| linear `<64>` (HEAD) | 1,104 B | 78 MB |
| linear `<256>` (HEAD) | 4,176 B | 295 MB |

- The tiers 16/64/256 came in with `a84f56b` "initial", never measured. `ENGINE_RECORD_LIMBS_MOST 256u` copies that tier; `CYCLE_RECORD_BLOCKS_MOST 4096ull` is invented. Neither is measured yet.
- The ladder's scratch `reached[WIDE + 2]` costs as much as the register file itself.
- `key_schedule_record_lay` gives every step its own register for the whole run and never reuses a dead one, so the file grows with every step.
- The first sweep of a run pays about 15 ms that later sweeps do not (15,221 us, then 550 us).

## Waiting on Doug

15. Answered 23 September: the unit sweeps become the default residual (tracker and `flatten`); the key stays for the proof. Done: `ENGINE_RESIDUAL_BY_UNIT_SWEEP` is 0, the zeroed default. `--residual-key` selects the key, and `--unit-sweep-prove` runs both and compares them. On 44b6_0113de3b by default: the unit sweeps take 1.41 s over 100 frames and the key 0; edges 38/12, unchanged.
16. Answered 23 September: `flatten` runs one frame at a time, as the tracker does. Done: `flatten_set` calls `engine_residual` a frame at a time (its own key, slab, atoms and device lanes are gone), so it takes the unit sweeps too. On 6bba_48816121 (scratch set): 7.7 s, the residual 1.5 s of it; components proved on 100 of 100 frames, 0 bits lost, 0 faces differ on the graded frame; `flattened.iapx` byte-identical (`cmp`) to the one the old slab wrote.
19b. Answered 23 September: the one microseconds clock is a static inline in engine_config.h; the base modules' copies and engine.cu's go. Done: `engine_clock_microseconds` lives in engine_config.h; max_tree, box_history, climb_machine and engine.cu no longer keep their own.
20b. Done 23 September: `climb_machine_extents(machine, const ClimbMachineExtentsRequest *)`; the review edits, the successor helper and the cast comments rebuilt clean and every proof held (C1 0 of 35,755; the box's three proofs 0; C2 0 of 11,780,208 and 0 of 9,021,600; the unit sweep 100 frames, 0 lanes; edges 38/12).
17. Answered 23 September: `engine_record_sweep` and its unchecked `*sweep_microseconds` stay as they are.
18. Answered 23 September: a cast across width or sign carries a one-line `//` comment saying what it widens, narrows or re-signs and why that is safe there. It is the one exception to the no-comments rule. Redundant casts are still removed.
19. Answered 23 September: fold both. The microseconds clocks become `engine_clock_microseconds`, and the key's successor builder becomes one helper in score_sample.cu.
20. Answered 23 September: reshape `climb_machine_extents` to take one const request struct.
21. Ruled 23 September: stateful graceful errors are the only acceptable error. Kinds: request, resource (a failed CUDA call or allocation) and logic (unforgivable; the result is withheld). Each request carries `EngineError *error`, and the engine keeps its own record too (first write wins). The record holds kind, module, site (line), status, execaddr, evacaddr, imagebase and up to 16 return-address frames. Builds emit a .pdb (`-Z7`, `-DEBUG`), so execaddr − imagebase steps straight to the source line. Wired so far, each built and checked on 44b6 (38/0/12, proved on 100 frames, 0 lanes differ):
- engine_frame_bodies; engine_residual (a BOTH_PROVED lane disagreement is a logic error);
- max_tree (every entry, slide and overlap included), flatten, decimal_double;
- unit_sweep, cycle (module 5), keymath (6), key_schedule (7), residual_program (8), grow (9);
- apxrep (10): its file layer (input, history and bodies read/write) takes an error. _IO errors carry errno; bad contents (header, bounds, CRC, trailing bytes) are logic errors.
- The engine calls that go through apxrep: iapx head/load/prove, ingest, entropy set/cloud/history read, and bodies read/write. The two set requests carry `error`, and the others take a trailing `EngineError *`.
- engine_entropy_set's keep path and schedule's per-sample skip each use a local error, because those reads may miss. The failure path was checked with a missing sample: module 10, site 178, status 2.
- compression (11) and tower (12): requests carry error; TOOK on every CUDA call. A malformed offset table or chunk count read from a file is a logic error. A coefficient overflow on lift is a request error; a rebuilt value outside 16 bits on lower is a logic error. entry_iapx_encode/decode take the error. Checked: 44b6 held, and iapx-prove proves 1 of 1.
- RSNA knee (a peer session) relayed Doug's answers:
  - zip becomes its own module, engine/base/representation/zip/, with the ZIP64 walk moved out of npy.c;
  - dicom becomes engine/base/representation/dicom/;
  - one sample is one SeriesUID, the single */<SeriesUID>/ prefix in the zip;
  - "do not bound or truncate the data": DICOM header bytes go inside the .iapx as a new section.
  The .iapx framing is still open with Doug.
- entropy_history (13): windows(extent, error); the project request carries error. Broken parity is a logic error. Checked on 44b6.
- Doug's rulings on .iapx v2:
  - sections in order of how often they're read (the file is read FIFO): head and geometry, then pixel offsets and stream, then deflated DICOM header bytes (which engine_iapx_load hands back), then the member list;
  - CRC-64 per part, with no zip CRC-32 rejoin.
- Doug accepted the counter-proposal put to him:
  - zarr is untouched; dicom is a source kind reading through the zip module;
  - EngineArrayRead gains an optional EngineSideBytes *side;
  - the zip archive is held across samples, keyed by path and indexed by series prefix;
  - equal slice keys break ties exactly by InstanceNumber, then SOP UID; nothing is refused;
  - no bounding: nothing is refused for rescale, signed pixels or transfer syntax (signed pixels become stored + 2^15, pending Doug on the mechanism);
  - flatten takes smooth/background orders per sample, and one set holds every geometry.
- NO ROUNDING (Doug, emphatically). The old zebrafish orders {2,34,34}/{6,96,96} came from chosen scales and n = 4(σ/v)² rounded to even: a debt, never to be reused.
- Ruled by Doug:
  - Parity (a): refuse an odd background order per axis (request error). An odd smooth order is held, with an exact half-voxel offset recorded as a residual field. The residual is 2^G·B_s x − B_b B_s x, so the centres differ by b/2. The guards to change are residual.cu:24, keymath.cu:118 and unit_sweep.cu:254 (narrow_bits at :259).
  - Count rule: order = period (n = P, both in voxel steps).
  - The per-axis coherence reading on volumes is new engine math, still to build (bench_recover_period is the 1-D model).
- Converter work order:
  1. the zip module (ZIP64 walk moves out of npy.c; module 14);
  2. dicom;
  3. EngineSideBytes on EngineArrayRead;
  4. .iapx v2;
  5. one series end to end, then ping RSNA (rsna-knee-abnormality-7d).
  The data: \\server\S\kaggle_project_data\rsna_knee\rsna-knee-abnormality-detection.zip (265 GB, 819,640 deflate members, train_series|test_series/<Study>/<Series>/<SOP>.dcm).
- Done 23 September: the zip module (14), dicom (16) and the engine wiring. Detection is PK + the member folder's first file carrying DICM. A file source maps sample to member, and zip_held_release runs at the end of ingest. Test series 1.2.826…903947 (34×960×960 u16) was ingested and proved from the file, CRC-64 45aea19b28ddd8b9, 38.1% of raw. An independent Python read agreed on the shape, the one prefix and 34 distinct keys. The key runs opposite to InstanceNumber. The set dir needed the 8.3 short path because set + two UIDs exceeds MAX_PATH 260; the engine's opens need a long-path form (to do).
- The peer's census: all 819,635 members are Explicit VR LE, 16-bit allocated, one sample per pixel, native. 201,276 slices are signed.
- Ruled by Doug:
  - signed pixels: sign-extend from HighBit to 16 bits, then add 2^15 into the u16 lane. Read BitsStored and HighBit; the .iapx records the offset. A slice whose raw words are not exactly the sign-extended form keeps its raw pixel bytes in the side bytes.
  - Deflate encoder: a full LZ77 + dynamic Huffman module, proved by inflate round-trip.
- Doug named the information crystal `.kcr` (Kolmogorov crystal) on 23 September.
  - "do not write version bridges": .kcr is the only crystal format. Old .iapx crystals are not read; sets are re-ingested.
  - The crystal's apxrep kind is "KCR\0", version 1.
  - Layout, read FIFO:
    1. the head (13 words) with its CRC-64;
    2. the offsets and limbs with their CRC-64;
    3. the deflated side bytes with a CRC-64 of the inflated bytes;
    4. the member tables and names with their CRC-64;
    5. EOF.
  - engine_iapx_load takes an optional EngineSideBytes *side (the headers are handed back, and lane_offset rides in it); engine_side_release frees it.
  - Names for the other apx files (flattened, OAPX history, BAPX bodies, IMP key) are still open with Doug. .kfc/.kst/.kgr/.ksd were offered; .ans is for the answer file.
  - The code identifiers still say iapx (engine_iapx_*, iapx_path); rename them to crystal.
- Deflate encoder module built (engine/base/representation/deflate). It does exhaustive LZ77 over the 32 KiB window with one-step lazy matching, package-merge length-limited codes, and picks the smallest of dynamic, fixed and stored. It round-trips 13 inputs through our inflate and zlib, landing within about 1% of zlib-9 on DICOM (no block splitting yet).
- Doug on output (23 September), answering "printf in the hot loop":
  - Port the needed MMgr scribo and laboro code (numeros_scribo, verba_scribo, cellularum_laboro in D:/git_project/repos/owned/public/MMgr/src) into our own simple string module. Do not link MMgr.
  - "zero print in any loop unless we raise our hand, else silently update your functionals returns, which get dumped into each print format and called on demand unless an error, so sorta like verbosity but never trap warn or error."
  - So: functionals fill result structs; one print format per result renders it through the string module on demand; errors and warnings always print.
- Doug: "we should be able to output iapx as any valid format, we compress it we can decompress it too." The export runs .iapx → any source format (dicom zip byte-exact from the side bytes, npy, nifti, tiff, zarr...). Build it after v2.

Still to wire:
- in the engine: tower, compression, entropy_history, stack, golden_bands, link_objects, run_cfg, run_log, cfg_json, relate_frames, double_fields, and cycle.c's host run;
- the entry_* ingest (source read, zarr and its codecs).
Cell-specific modules (bodies, score_sample, answer_key, the measure/*) are due to move to cell_tracking first.
22. Ruled 23 September: CRC every schedule, cfg and key at imprint and verify right before each cycle; CRC every disk read and write, with a flush read-back. Size the buffers generously, by experiment, to match ingest and r/w throughput (Doug suspects a hardware queuing "snap io desert"). Not started.
23. Ruled 23 September: `decimal_double` never rounds. `decimal_double_expand` gives the exact leading bits of digits × 10^ex in an `AnchorExactInteger` candidate, plus a binary exponent e2. It also reports `fits` (the candidate holds the whole value), `terminates` (the binary expansion ends; a tenth does not) and `needed_bits` (the width to grow to). Checked against Python fractions on 22 cases at 128 and 4096 bits: 1e2200 needs 5109 bits, and a 4096-bit build gets its exact top 4096 with fits 0.
24. Ruled 23 September ("split it, go"): the slide and the overlap are engine math and are back in max_tree, wired for errors. The slide's key voxels became generic probe voxels, and the engine returns each recorded level's probe partition. The present/alone tally left the engine for cell_tracking, and a slide count that disagrees with the tree's is now a logic error. The overlap keeps probes (points) and probe links (point pairs) as its only interface. The key-cell grading that goes with this item is the cell program's (the cell book's build plan, item 24).
25. Ruled 23 September, integrity: "the only acceptable crc is a merkle dag". Every CRC-64 is replaced by BLAKE3 Merkle roots. Doug's answers:
- root: the full 32 bytes;
- shape: dimensional, leaf = one x-row of LE u16 lanes (its own width, BLAKE3 chunks it), then y, z, t, sample, set;
- stored levels: "every floor of the tower", so every level's nodes are stored, rows included (about 6% of a knee crystal at 512 columns);
- scope: "every crc64, we need 100% idempotent determinism against cosmic flips" (head, side bytes, members, stream, parts, sets, schedule, keys, flattened, history, bodies);
- the universal root: "literally every file the engine touches plus one on itself" (inputs read, outputs written, and the engine's own binary);
- placement: "Measure both, report": the hash section after the head against a footer, write cost and one-plane check cost on the knee set;
- repair: fail closed and report the path (sample, t, z, y, row); no parity unless Doug asks.
- Module name (Latin, to avoid colliding with a user's libblake3): "obsignatio aeterna, then you can use the signum signator nota notarius sigillum sigillator and comprobare officiale or variation". The module is `obsignatio` (engine/base/representation/obsignatio, ENGINE_MODULE_OBSIGNATIO 17).
- Built 23 September: the BLAKE3 port. `obsignatio_signum` (host, any length, extended output) and `obsignatio_many` (device, many equal-length messages at a stride; chunk kernel, then level kernels pairing nodes with the odd one carried up, which is BLAKE3's left-heavy tree). One `__host__ __device__` compression core serves both. Modes: hash, keyed, derive-key context and material.
- Doug: "the hashing function needs a full test/ suite for validation ... we just make it happen and prove it." Suite at obsignatio/test (run.sh, obsignatio_test.cu, the official BLAKE3 test_vectors.json). All pass, 0 failures: host vectors 210 (131-byte extended and 32-byte, three modes); device vectors 1,260 (offsets 0/1, strides L/L+1, three messages, three modes); device context 1; every length 0..102,400 host against device in three modes, 307,203; every single-bit flip of every vector input, 1,807,226 (each differs from the original, all distinct); determinism 2; fail closed 20 (refusal, error kind and module, output untouched).
- DAG node format, ruled 23 September: each level is separated by BLAKE3 derive-key, one context string a level, of the form "obsignatio aeterna 2026-09-23 <level>" (a later format change takes a new date); every node at a level is hashed in keyed mode under that level's derived key. The sample root seals every section: H(head words ‖ lane-DAG root ‖ stored-stream root ‖ side-bytes root ‖ members root), where the stored stream has per-chunk leaves over the compressed limbs so a flip on disk localizes before decoding.
- Mutation proof, 23 September: five mutant ports (a block's last byte dropped, the host stack merged on the wrong count, the device carry zeroed, no ROOT flag, the chunk counter held at 0) all fail the suite, each where it should. The every-length and flip checks cannot see a core bug shared by host and device; the official vectors catch those.
- Built 23 September, the lane DAG: `obsignatio_level_context`/`obsignatio_level_key` (12 levels: row, plane, volume, lanes, chunk, stream, side, members, sample, set, file, universal) and `obsignatio_lanes` (rows → planes → volumes → lanes root, each level one `obsignatio_many` over the level below; nodes laid out rows, planes, volumes, root, `obsignatio_lanes_nodes` = tzy + tz + t + 1; OBSIGNATIO_EXTENT_AXES 4 in the header). Tests: level keys 16, lanes reference 7 shapes against a host build node by node, lanes locality 9,234 single-lane flips each changing exactly its row, plane, volume and the root, lanes closed 10. All 0 failed.
- CRC-64 sites to replace (crc.h, crc_key.h): apxrep, engine.cu, tower (key and schedule), entropy_history, flatten, score_sample, the driver's prints. Format-intrinsic checks stay (zip CRC-32, PNG, inflate/zarr).
- Ruled 23 September: a stored-stream chunk leaf hashes the chunk's exact bits repacked to start at bit 0 (LSB-first, as compression writes them), prefixed by its bit length; leaves are disjoint. The side root covers both: one leaf over the stored deflated bytes and one over the inflated bytes. `member_crc` is the zip entry's own CRC-32 (a zip field the byte-exact export needs) and stays. apxrep may not reach obsignatio, so engine.cu computes and checks every root and apxrep only frames the node bytes.
- Doug on tamper protection (the Cython dependency he will have this compiled to): "the redundancy check happens in one swept cycle with every other process simultaneously". The caveat given: an unkeyed root can be recomputed by whoever can edit the files; tamper resistance needs the universal root anchored (an Ed25519 signature with the public key compiled in, or a published root). Open for when the universal root and the Cython build come; keep every hash level a batched device pass so it can fold into the swept cycle.
- Doug, 23 September, the flow: "ingest->seal kcr shards until we accumulate the grains to a lattice and the same goes for the output and any scheduling, we coalesce a universal hash for the state of the program, so this works when users input their own data, they seal it themselves by running." Each .kcr is a shard sealed at ingest; shard roots fold into the set (lattice) root; outputs and schedules seal the same way; one universal root is the program's state. The engine is deterministic, so rerunning the same input on the same engine must give the same universal root (a reproducibility proof). Doug on its origin: a one-bit flip anywhere in the monolith already stops the program ("on ACCIDENT"), because every stage is exact and chained; the seal makes that stop name its place.
- Built 23 September: `obsignatio_bits` (chunk leaves: bit-range messages over a limb stream, LSB-first, each hashed as an 8-byte LE bit length then the bits repacked from bit 0; a check kernel refuses malformed offsets before any write; one thread a message runs the shared stack digest). The core reads message bytes through a source (plain bytes or a bit range), and the host `obsignatio_signum` and the device share `obsignatio_digest`. Tests: bits reference 32 (boundary lengths 0, 1, 7–9, 31–33, 63–65, a BLAKE3 chunk ±1, two chunks, first message at bit 5; hash and keyed), bits locality 57,216 (every bit of the stream, padding included), bits closed 11. Whole suite 0 failed.
- Doug: "our specific engine signature gets hashed and stops the program if its different". The engine hashes its own binary at start and refuses to run on a mismatch. Open: where the expected hash lives (it cannot sit inside the binary it hashes unless that slot is zeroed while hashing; or a sealed manifest beside it, which brings back the anchor question).
- Doug: "we do compile time injection for the engine hash". Open detail: the injected value is either the linked binary's hash with the injection slot zeroed (patched in after the link) or the hash of the engine's sources/objects computed by the build as a define.
- Found 23 September: the tower transform spans all four axes, so one plane cannot be decoded alone. A partial check without a full decode is possible only on the stored bytes (chunk leaves); checking decoded rows costs a full decode, though a mismatch still names its row. Matters for the placement measurement.
- Stage A (in progress 23 September): the .kcr seal. engine_config.h has EngineSignum (32 bytes) and EngineSeal (roots sample, stream, side, members, side stored, side inflated; lane nodes; chunk leaves); EngineStream.crc and EngineSideSection.bytes_crc are gone; EngineHistory.sample_crc became EngineSignum sample. Stage B: history payload/cloud, bodies, tower key/schedule CRCs.
- Stage A done 23 September: the .kcr seal built, the suite rerun with 14 levels (2,182,450 cases, 0 failed), tamper 9/9, the theory ([obsignatio_seal.md](obsignatio_seal.md)) and the table's M13 and A10 written.
- Placement measured and ruled 23 September: both alike on the knee (25,218,496 bytes each, prove median 76 ms each over 15 alternating runs). Doug: after the head. The at-end placement, ApxrepSealPlace and --seal-at-end are removed.
- Witness ruled 23 September. Doug: "we ingest seal and then witness pixel:pixel 1:1 once. then we have our hash and we know our input is verified and witnessed." Ingest witnesses twice (device voxels against the in-memory source, then a second independent source read pixel for pixel on the host); Doug: keep both.
- EngineSampleRecord.crystal_read added: a file that apxrep refuses structurally now prints as not read, not as "0 differ". Verified on the rebuilt engine: a re-ingest wrote a byte-identical crystal, prove 76 ms, tamper 0 cases wrong, the old at-end file refused as not read.

26. Ruled 23 September, device-job scheduling (Doug: "just a normal job scheduling function, super common for stuff like this"). Module name `tessera`. Doug's answers:
- jobs are packed by memory, several at once while they fit (not one owner);
- the device share is a request field: the caller names the byte budget, the engine never chooses one (schedule.cu's free/3·2 is a chosen constant to remove, listed in the scale audit);
- one arbiter per host/device: "daemon or systemd on linux that self tears down/knocks up, single win pid, single docker hook", to be deployed onto a SAN;
- clients talk to it over a local socket (named pipe on Windows, Unix domain socket on Linux);
- measurement: NVML per-process device memory by pid, "with periodic dead sweeps ... otherwise itll leak over time and lock down memory that is never used";
- stale tickets "get dumped to lostandfound";
- budget: "caller declares, we measure, they overbudget it could be for a reason we ask they can override, they underbudget we grow and report via warn so accounting isnt perturbed";
- over-budget is asked at admission, "but dont throw it away, we have a complete precalc tossing would be a waste, it goes into holding before stalling and dumping to lostandfound".
- The peer sessions (RSNA, CASMI) use pinned git dependencies; see [peer_sessions.md](peer_sessions.md).
- Over budget is judged against the job's signum's last measured peak: a job is keyed by the BLAKE3 signum of its request (the engine is deterministic); tessera keeps each signum's measured peak; a never-seen job is admitted on its declaration and measured.
- Times are per job fields (the holding time before lost and found, and the sweep and idle times travel in the requests; the engine chooses none).
- First build: all of it ("all of it, use websearch"): the daemon and client over the local socket (Windows and Linux), the driver wired through it, the service files (systemd unit, Windows single instance, docker hook), and a tessera/test suite.
- Queue order: backfill with the head kept (a smaller job goes ahead only if the head's admission is not delayed).
- Times: "what I want is the shortest time to change of anything to be the root because that is most likely event, in order". Every deadline (each job's sweep, each held job's holding expiry, the idle teardown) goes into one min-heap keyed by time to change; the daemon waits on its socket with the root's remaining time, handles the root, re-inserts, in order.
- Lost and found: on a stall the daemon tells the client, and the client writes its precalc there, sealed, beside the ticket, so it can be resumed.
- Doug, 23 September: "see how some of our ingest operations are a tower themselves, and can get crammed into one prep cycle, that will save an enormous amount of time". Ingest reads one lattice five times with different operators (tower lift, source lane seal, decode, rebuilt lane seal, voxel compare); fuse them into two sweeps. Measure the ingest's stage times first.
- Doug, 23 September: "you should fix any defect found".

27. Ruled 23 September, the engine reorg: engine/base/ (never sees a body: lattice, representation, codecs, the tower, obsignatio, exact arithmetic, scriptura, tessera), engine/nbody/ (defined over n ≥ 1 bodies, n = 1 the base case; no single/ folder), engine/sims/ (programs composing them: run_cfg, cfg, tsv, nbody_program, the oracle). The test: does the module's request carry a body count. The engine README lays out the six steps (Doug: "yep"): 1 ingest (source → lanes), 2 seal (lanes → signa, the witness), 3 lift (lanes ↔ crystal: tower, compression, apxrep), 4 measure (lattice → fields: residual, moments, entropy), 5 partition (fields → bodies: max_tree, grow, bodies), 6 relate (bodies × frames → links). Each file names the step(s) it belongs to. Move by paste then delete; git index untouched.
- Then: the anchor_sift port. Doug, after the table was done: "have them read our engine, and discuss the move together, did we miss something? if so it gets added to the table and implemented before moving so as to not disrupt or disrupt them as little as possible, they do not know how to use this engine it is foreign to them completely it has evolved". Messaged "DMA plan reconciliation (2)" [ce24e8] on 23 September with the reading list (engine/README.md, engine_table.md, obsignatio_seal.md, tessera_scheduler.md) and three asks: their use, the gaps, where it lands. Nothing moves into their tree until agreed; every gap goes into the table and is built here first.
- Reorg done 23 September: 66 modules moved (copy, diff -r, then delete), build scripts and docs repointed, engine/README.md gained the six steps and every file's step(s) (in the README, since files carry no comments beyond SPDX and cast lines). Every defect in the "Found" lines is to be fixed, not just listed.
- Still to do: stage B (history payload/cloud, bodies, tower key/schedule crc_key.h, flattened, the driver's fingerprint print), then the universal root and its file, the engine self-hash, and the anchor.
- Found on the way, 23 September: a prove where no sample held still prints "the set's root, sealing every sample's in the order named" over zero samples; the root is only meaningful when every sample held.
- Found by the CASMI session, 23 September: host cycle.c is compiled at the library's 16-limb width and refuses a 28-limb step that the device accepts (within ENGINE_RECORD_LIMBS_MOST); host and device widths disagree. Report to Doug.
28. Built 23 September: engine/sims (Doug: "engine simulation files, just like in anchor_sift engine"; categories noise-floor, synthetic n-body lattice, period, anchor_sift art). `bash engine/sims/run.sh <sim>`; shared sim.h (tally, keyed draws, exact rationals via the exact integer) and sim_camera.h (camera law: Binomial(4S,½)−S shot, Binomial(4r²,½)−2r² read, fixed pattern, gain, moving/dividing ellipsoids with truth). Results: nbody_lattice 9/0; noise_floor 11/0 (BM recovers every seeded stream, planes 0–8 random, truth PTC slope 2.0046 vs 2, data-only routes biased 7.0 and 4.5 vs 1 by moving bodies); period_power 599/1 (fails on purpose, see 29); fixed_pattern 10/0; classify_reject_recover 7/0. Facts sent to the TeX writer. The engine has not been run on the sim's lattice.npy yet.
29. Ruled by Doug 23 September ("fix all of them"): both period findings fixed and proved. (a) The period is now the smallest candidate whose height clears the band top (the fundamental), not the strongest peak: from amp 64 up, 32/32 volumes read 5, 0 multiples (was 8/20 at amp 32). (b) The null is a per-axis line shuffle (each line along the axis permuted alone, keyed Fisher–Yates), not a global Feistel: z/y false periods at amp 1024 fell from 23/64 to 5/64 at 8 draws, 2/64 at 19. period_test 700/0 (host reference rewritten to the same rule), period_power 599/0 (the deliberate-fail check now passes). Old global-shuffle and strongest-peak behavior superseded. Timed 23 September (scratch program, RTX 3070, 34×960×960 smooth ramp + noise): period_draw 455.9 ms a call, marginal draw in period_read 450.1 ms, fixed 133.7 ms; 2.96× the global shuffle's 152 ms. At 73,158 draws a series that is ~9.1 h a series, ~223,000 GPU-hours for the set (was ~75,000), so the set-wide band is even more necessary. period_draw allocates per call (not separated). Sent to the TeX writer.
31. Ruled by Doug 23 September ("increase the scheduler steps to n" + "implement the lut"): done. (a) ENGINE_RECORD_STEPS_MAX decoupled from ENGINE_RECORD_LIMBS_MOST, raised to 1024 (the sign array's local-memory cost is the bound; scale audit). (b) key_schedule gained opt-in register reuse (KeyScheduleRecordRequest.reuse / EngineRecordRequest.reuse): liveness free-list frees a register once its last reader has run, so a long chain fits the 256-limb file (a 21-step chain: 3 limbs reused vs 21 kept). Default off → proven programs byte-identical. (c) ENGINE_RECORD_TABLE (op 10): a lookup table, where the low index_bits of a register select an entry; the engine's nonlinear edge (kolmogorov_arnold.md). Threaded config→keymath→key_schedule→cycle (device + host). Tables compose by reading one through another (in order; Doug: "collapsed in order onto" the 2D floor). Test test/record_table_test.{cu,sh}: 15 checks, 0 failed, using the ordinary ops as oracles (x², |x−30000|+100, composition g∘f, reuse, refusals). engine.h EngineRecordRequest gained tables/table_count/reuse.
30. Port survey (23 September): every anchor_sift engine C/CUDA file present here is code-identical or ours is larger (max_tree, climb_machine). Missing by name: the n_body basin modules (removed 22 September in the v2 split, superseded by the cycle/keymath residual), the sha256 tree (the miner's), and the MATLAB/R/Python ports. Nothing ported back without Doug; the art detector (anchor_sift's periodic_energy) now exists as sim code.
32. Ruled by Doug 23 September ("the tower is its own object, the lut is its own object, if we integrate them both we get a root universal"; chose "bijective edges in stream"): done. TowerEdge {floor, index_bits ≤ TOWER_EDGE_INDEX_BITS_MOST = 20, forward} in tower.h; TowerLiftRequest/TowerLowerRequest gained edges/edge_count (callers memset → no edges → the tower as before). An edge permutes the low index_bits of every approximation coefficient at floor k (the region after k halvings; floor == floors is the collapsed floor), high bits pass through, so it is a bijection on the word; tower_lift lays E0,L0,E1,…,L(N-1),EN and tower_lower undoes them as EN⁻¹,L(N-1)⁻¹,…,L0⁻¹,E0⁻¹ (a floor's edges unwind as a stack). Validated up front (a permutation, width 1..20, floor ≤ floors), refused before any device work. test/tower_edge_test.{cu,sh}: 20/0. Sim engine/sims/root_universal: 1268/0. Exact through lift→code→wipe→decode→lower on 48 camera volumes (4×12×48×48, 3×13×37×41, 171 edges incl. 20-bit). Fold 8/8: a,b on one floor = b(a); b,a differs; a floor between them blocks the fold. Price (coded bits/voxel, plain 6.802): floor 0 at widths 2/4/8/12 → 6.810/6.908/8.719/12.608; middle floor (72 coefficients) ≤ 6.817; collapsed floor 6.802 (free). So an unfitted edge is exact but costs the magnitude coder unless it sits high in the tower or is fitted, which feeds the open "which functions to tabulate". Engine rebuilt and published. Not yet: the edges are not wired into engine.cu's crystal/seal path (the .kcr would have to record the edge program). Doug's call.
33. Doug 23 September ("reaching a boundary here means we need a dimensional expansion to realize the transform"; approved): the Bennett embedding (x, y) → (x, y ⊕ f(x)) makes any f a permutation of the widened field, so a lossy table rides the tower's reversible stream at the price of width (A14's "only permutations" bound becomes a width statement). No engine change; tables built on the host. tower_edge_test now 25/0: |x| and compare as 16-bit Bennett edges round-trip on 64 lattices, f(x) reads out of the carrier on the collapsed floor, the edge touches only its coefficient, mid-tower Bennett edges round-trip, |x| laid bare is refused. Naimark/Stinespring/purification are the quantum forms of the same expansion, which generally bring square roots.
34. Doug 23 September (terms approved: ask, state, answer distribution, magnitude, phase, complete ask, valid set, crossing rule; "represent square roots as their real number with their square operation"; "why would you use 64 bit?"): sim engine/sims/ask_state, 33/0. Exact rationals on the exact integer (word-gcd reduced when they fit, exact unreduced otherwise), Q(√3) numbers as rational + root·√3 with (√3)² = 3 and exact signs. The rational tetrahedral ask (|v|² = 3/4, r = 4Σpv) and the true SIC (|v|² = 1, r = 3Σpv): 25 states (21 pure, 4 mixed) return exactly; pure on the boundary, mixed inside; crossings 75/75; the phase: 14 equator states all ½,½ on the 0/1 ask, all 91 pairs told apart; the weights onto +x are 3/2, −1/2 and ½ ± ½√3 (negative, not classical); (1,0,0,0) refused, crosses to 3/2 and ½ + ½√3; grid 455 at denominator 12: 55 and 87 states, 104 and 72 non-states that still cross to probabilities on x, y, z; the two asks cross into each other 25/25. Host exact arithmetic, no GPU work. Next on Doug's word: KA ψ with symbolic exponents (Doug: "the exp is symbolic after the second nth exp"; a sparse (digit, exponent) form, costs polynomial in the scale); the tower_lower hat-function limit; camera-parameter recovery sim.
35. Doug 23 September ("Yes" to the ψ sim; "the exp is symbolic"; "our boundary is truthy falsy ... with mpprec"): sim engine/sims/ka_psi, 114/0, 28 s. Values at depth are sparse sums c·γ^−e with e = β(L) held as an integer and never expanded. The exact sign comes from the head plus a proven tail bound, decided by bit lengths when the gap is wide. Results:
   - Sprecher (2.5) is exact: 2207/4000 > 11/20. It descends on 10 of the 99,999 pairs of D_5, the first at 0.09000.
   - Köppen, both readings of the carried midpoint: (2.9) adds (γ−2)/2 units (symmetric) and (2.7) as printed adds (γ−1)/2. For n = 2 and n = 3 at γ = 10, on every point of D_1..D_5: the pair step equals the level recursion, ψ is strictly increasing, the least gap is exactly γ^−β(L), and the widest gap equals the recursion G_L = ½G_{L−1} − (s/2)u_L, with s = γ−2 for (2.9) and γ−3 for (2.7).
   - The chained step, checked symbolically to L = 60 (β = 2^60−1) and L = 38 (β ≈ 6.75e17): order kept (u_{L−1} > (γ−1)u_L), least gap kept (u_{L−1} > (γ+1)u_L), widest gap > least, ≤ 2^−(L−1)/γ (continuity) and within Lemma 2.3. The symbolic G_L equals the measured one on D_2..D_5. A keyed depth-L point's gap lies between the least and the widest.
   - Both readings give a continuous, strictly increasing ψ; the discrepancy between (2.7) and (2.9) changes only the widest gap.
   - Separation (Doug "Ready go"), now 152/0. ξ = Σα_pψ(x_p) was computed exactly on D_k^n (n = 2: k ≤ 3, 10^6 points; n = 3: k ≤ 2), with the α tails bounded.
     - LEMMA 3.4 IS FALSE AT k = 1. ξ(0.1, 0) − ξ(0, 0.9) = 1/10 − (9/10)α₂ = 0.0090999… < γ^−2, and the n = 3 analogue gives 9.0998 units. Lemma 3.8's supports (ramp γ^−β(k+1)) overlap at k = 1.
     - The proof's break: (3.11), the α cut at r ≤ k, does hold. It measures exactly 10, 50 and 500050 units for n = 2 at k = 1, 2, 3, and 10 and 5000 units for n = 3 at k = 1, 2. But "the tails cannot annihilate it" does not mean "the tails keep it at or above the bound".
     - Correct: |µ_k| ≥ γ^−nβ(k) − Σ_{p≥2}ε_{k,p}, about (γ−1)u' with u' = γ^−β(k+1). That beats the image width (γ−2)b_k ≈ 8.81u' (n = 2) and 8.88u' (n = 3). With ramp ρ = γ^−(β(k+1)+2), the supports are disjoint at every k: checked symbolically for k = 1..57 (n = 2) and k = 1..35 (n = 3). The paper's ramp is covered at no k by this bound.
     - Measured from k = 2 the true gaps are far wider (49.5, 500049.5 and 5000 units). The shifts check: m + 1 = 2n + 1 shifts never share a gap; with m + 1 = γ they do.
   - Doug ("You have division", "2s compliment", "Mulmask exact", "add that to the exact arithmetic"): engine/base/no_rounding/exact_integer gained three functions, and test/exact_divide_test passes 8/0.
     - anchor_exact_divide: Knuth D in base 2^32. It returns the quotient toward zero and a remainder with the numerator's sign. The add-back branch was confirmed to run, using an instrumented scratch copy.
     - anchor_exact_divide_exact: the mulmask exact division. Twos shift out; the result is the odd part's inverse mod 2^(32·limbs), by Newton x(2 − dx) with a two's-complement negate, then a multiply and a mask. It is multiplied back, and a remainder is refused.
     - anchor_exact_gcd: Stein's binary gcd.
     - New statuses ANCHOR_EXACT_BY_ZERO and ANCHOR_EXACT_NOT_EXACT.
     - sim_rational.h now keeps every value in lowest terms at any width. ka_psi (152/0) and ask_state (33/0) are unchanged in their verdicts and now print exact fractions (2207/4000, 11/20).
     - Then Doug: Schönhage–Strassen, "We use karatsuba", "Expand the header to accept any n bits power of 2", "Static assert".
       - The header takes ANCHOR_EXACT_BITS or ANCHOR_EXACT_LIMBS: any power of two ≥ 32 bits, no ceiling. The limbs/bits pair is derived and must agree; DIGITS was briefly min(1024, what fits) and is back to anchor_sift's fixed 1024 floor (see the integration below). All checked by static asserts, and the ladder rungs are static-asserted too.
       - Width-sized working copies are compile-time rooms: stack up to ANCHOR_EXACT_STACK_LIMBS = 4096, heap beyond, WILL_NOT_FIT if not held.
       - Ladder: long multiplication < ANCHOR_EXACT_KARATSUBA_LIMBS = 32, then Karatsuba, then the SSA transform (ring 2^n+1, shifts as twiddles, ψ weighting for the negacyclic product, depth by an integer cost model) from ANCHOR_EXACT_TRANSFORM_LIMBS = 8192 (the measured crossover; the transform is 1.44× ahead at 16K, 1.55× at 32K, 1.85× at 65K).
       - gcd is now Lehmer (Knuth L) and returns a status. The exact division's Newton inverse doubles its precision and runs on the ladder.
       - test/exact_transform_test passes 3/0 at 128 limbs, 4096 limbs and 4,194,304 bits (operands to 65,536 limbs).
       - exact_divide_test passes 9/0 (it adds gcd = Euclid on the long division). ka_psi passes 152/0 and ask_state 33/0.
       - Newton division BUILT (Doug "Add the newton port"): a half-precision recursive reciprocal, one Newton step, exact correction. ANCHOR_EXACT_NEWTON_LIMBS = 8192, measured 2n/n on the Karatsuba ladder: Knuth leads to 4096; Newton is 1.21× at 8192, 1.06× at 16384, 1.81× at 32768 and 2.44× at 65536. exact_transform_test passes 4/0 at 128 and 4096 limbs.
     - Doug ("write these exact limb functions as computer key math primitives ... projected into a bit array of the same dimensions"): record ops ENGINE_RECORD_QUOTIENT = 11, REMAINDER = 12, GCD = 13, EXACT_QUOTIENT = 14.
       - Wired through engine_config.h, keymath imprint (bits: quotient = left, remainder = min, gcd = max), key_schedule refs, cycle.c (host, the library itself) and cycle.cu (device Knuth D, Euclid, and multiply-and-mask on the register arrays).
       - The kernel is templated <WIDE, DIVIDES>, so only a dividing program carries 5·WIDE+4 limbs of scratch.
       - test/record_divide_test passes 15/0: device = host word for word in the 64- and 256-limb files, with identities, exact factors, refusals and imprint order. record_table_test still passes.
     - Doug ("integrate anchor_sift's engine so we can copy push ours to anchor_sift"): THIS TREE IS NOW THE SOURCE.
       - build_engine.sh and build_driver.sh default ANCHOR_EXACT_ROOT to $TOP/engine/base/no_rounding.
       - 18 files (no_rounding arms, nbody/anchor_sift, render) were code-identical to anchor_sift's; ours had lost doc text to a sloppy prose pass, so anchor_sift's versions were copied in.
       - exact_integer.h/.c were merged by hand: our code with anchor_sift's contract, and stale notes fixed. ANCHOR_EXACT_DIGITS is back to the fixed 1024 floor (a narrow width must declare it; checked 1..64 limbs, and undeclared at 8 limbs is refused by name). check_exact_limbs.py's regexes still find the literal #define lines.
       - The engine DLL builds. Nothing has been written into anchor_sift's repository yet. A peer session "engine git dependency integration" exists and may overlap.
     - Doug ("Compute chaitins constant"): sim engine/sims/chaitin_omega, Omega for the BLC normalization machine (a closed term halts when it has a normal form).
       - Closed terms are unranked from exact counts (checked against a parse of every code up to 22 bits) and run by normal order on 16 threads, with Brent watching for loops.
       - The tail past L is counted in directed fixed point: parse mass 1 = the smaller root of T² − 3T + 2. Normal forms past L add to the lower bound.
       - L = 30: 0.122543 ≤ Ω ≤ 0.125993, 2 bits. (0.12597 was a hand-conversion error, caught by the writer.)
       - Doug ("everything is truthy falsy steer", "loop with no answer after information space has been searched is an exit condition"): the exits are now halts / loops / grows forever / out of steps / outgrew the space.
         - Bounded space ⇒ halt or repeat (pigeonhole): at 1e6 steps, "out of steps" was 0 through 30 bits.
         - Growth proof (omega_grows_forever): a spine part S, reduced only by head steps at spine position ≥ its own, returns deeper on the spine as S B⃗ ⇒ no head normal form ⇒ no normal form. It closes 213 of the 587 unresolved terms through 30 bits (200k steps, 16384 tokens). 64× more space moved none of the rest into halts or loops. The BB table (most steps; BB lambda = the largest normal form) matches BusyBeaverWiki through 22. 11/0.
       - L = 40 (283,817,255 terms; 488,151 loops, 474,630 open): 0.12407 ≤ Ω ≤ 0.12599, bit 3 NOT settled. The lower bound needs +0.00093 to pass 1/8, about L ≈ 48, roughly 250× the work: a GPU port or a larger counted halting class. Published: Calude–Dinneen 43/40 bits on their register machine; Tromp's own Ω_λ = .0001… (a different machine); a cryptid at 74 bits.
       - GPU PORT BUILT (Doug "Do it"): omega_device / omega_kernel in chaitin_omega.cu, one term a thread, the host's decisions in the same order; device budgets ≤ 256/256 (steer: anything reaching them, or outgrowing the 2×tokens room, is run by the host under the full budgets, exact since budgets only stop a run). Host handback settled while the next launch runs. Host cross-check through 30 bits (fates + BB + champions) passes; `cpu` arg = host only.
         - Speed: budgets barely matter for Ω (36 bits: 256/256 vs 2048/2048 exact per writer: lower −3.19e-9, open +2.50e-8, upper +2.18e-8 ≈ 2^-25.5; gap 0.00240 ≈ 2^-8.7; the earlier "2^-22" was a misread)
         - LEDGER BUG (writer reproduced): a line cut inside field 16 then ended by the writer's '\n' parses whole and wins as first match → wrong champion past 30 (fates/bracket safe). Fix pending Doug's go (he said Stop): end marker/checksum, or drop lines the writer had to end. Doug wants the lambda Ω run BY THE ENGINE (record machine), no step/token/width limits; awaiting his go., so Ω runs use 256 256. RTX 3070, 384 threads/SM (cache-bound; more is slower): 44 bits 3.39G terms in 28 s (121M/s, ~7.5× the host at 256/256).
         - L = 44 (256/256): 0.124438573589 ≤ Ω ≤ 0.125987962504; lower short of 1/8 by 0.000561; first typed halt (1). Upper barely moves with L, so bit 3 settles only from below (needs Ω ≥ 1/8); lower's per-bit gain shrinks ~0.93/bit ⇒ crossing ≈ L 54 (~3.7 h device) if the trend holds.
         - BB check split: "≤ published, = where settled" always; strict equality only at budgets ≥ 2048/2048.
         - JOBS (Doug: "why aren't you planning these as jobs for the engine and chunking"): `--ledger <path>` plans each length in 2^28-term jobs; each finished job's exact tally is one flushed line; reruns keep whole lines under the same budgets and run the rest (resume; longer L costs only new lengths). Cut lines are ended before appending; a line must parse whole and its fates sum to its count. Tested: fresh 31 → extend 33 (2 run, 27 kept) → rerun (29 kept) identical; cut last line → 1 rerun, identical.
         - RUNNING: `omega_jobs.exe 54 256 256 --ledger build/omega_ledger.tsv` (~3.7 h est.). Read a shorter L's bracket from the ledger any time by running that L with the same ledger (all kept).
   - Next: the outer-function contraction (Theorem 3.3) with the narrow ramp; ψ at the shifted points x + qa (non-grid, a = 0.0qqq… in base γ).
- Found on the way: a series name matching no DICOM member falls through to the npy reader (error module 15) instead of a "no such member" refusal; a failed ingest leaves the empty sample directory it made (engine_directories_make must report what it made, and its walk[1024] is a chosen bound, with the long-path work).
36. Tessera daemon, 24 September. Built and proved; moved to engine/daemon (Doug: "Put it in engine/daemon"); README on starting it and submitting jobs. Seal done (Doug: "Go"):
   - The history is records plus an obsignatio_seal (host keyed BLAKE3, file level). A load whose length breaks, that has no seal, or whose seal fails is refused and the daemon exits.
   - Failed saves are reported on stderr.
   - Tickets end with `seal <hex>`; the precalc note needs a seal that holds and seals the ticket again.
   - The job test runs in a scratch TESSERA_STATE: 24/0, including damaged, cut and unsealed histories refused and the kept peak surviving a restart.
   - The old unsealed real history was moved to %LOCALAPPDATA%\tessera\<uuid>\history.unsealed.
   - The history now loads before the pipe or socket exists, and the note uses s_daemon_precalc. Rerun at 24/0.
   - Doug: "The engines own runs need to go through tessera". The driver's jobs are the cell program's (the cell book's build plan, item 36).
   - Sims submit (24 September), each 0 failed with its tessera checks:
     - sim_job.cu with sim_job_submit/sim_job_release; sim_close releases; run.sh builds the client and the daemon beside the sim.
     - Declared and peak: period_power 262,144/145,915,904; nbody 26,542,080/173,178,880; noise_floor 9,142,272/187,858,944; root_universal 339,510/152,207,360; fixed_pattern 141,056/143,818,752; classify 118,016/145,915,904.
     - Ω now builds (the capture fix was in) and runs on the engine at L 16 as a job: 16/0, peak 286,425,088.
     - Nothing loads the DLL. cell_shift is anchor_sift's engine.
   - Linux run (24 September, WSL 2, gcc 13.3, CUDA 13.3). Transport: a tree from a scratch index, then git archive piped to ~/tessera_linux; /mnt is banned by the hook.
     - Fixed: PATH_MAX via linux/limits.h; _GNU_SOURCE in the measure; the timer's return; ENGINE_NOINLINE_HELPER; platform strings guarded; the daemon uses its string table, and the ticket names its signum.
     - WSL's NVML lists no process's memory, so tessera_measure_paravirtual refuses and the daemon exits. The suite passes with 0 warnings.
     - Native Linux is not available here.
   - The crystal's growth between 22 and 24 September is the compression book's (its build plan).
   - Service files (24 September): engine/daemon/service/tessera@.socket and tessera@.service (systemd user units). Activation was proven in WSL: systemd started the daemon on a client's connection, then the WSL measure refusal ended it. Windows needs no service. The docker mount is documented, not run.
   - Reservation rule done (24 September, by the stop hook's "finish all open items"): tessera_ledger_wants = max(declared, kept peak) is used for admission, the shadow, the spare and the reservation at start. The ledger budget test is 22/0; the job test is 24/0. The real prove reserved 3,958,566,912, its kept peak.
   - Docker mount run (24 September): Docker Engine 29.1.3 in WSL; the static test/tessera_socket_probe.c in a container, socket mounted, TESSERA_RUNTIME=/run/tessera. systemd started the daemon on the container's connection; the WSL refusal ended it.
   - Open: native Linux (no native NVIDIA Linux driver on this machine); the is not run; no service files; engine runs don't submit through tessera (M14); the Ω engine-run path (lambda capture at about chaitin_omega.cu:2788) is unbuilt.
   - WSL self-report (24 September, Doug: "Use wsl"): under WSL each client reports its own device bytes (dxcore, `tessera_self.c`, TESSERA_ASK_MEASURED); the WSL job test is 24/0. The systemd job test failed on its first pass because a daemon left over from the suite unlinked systemd's socket file at idle. Fixed in tessera_daemon.c: a daemon probes the path before binding and refuses if something answers, and it unlinks only the file it bound (dev/inode). After the fix: 3 of 3 passes 24/0, the Windows suite passes, and a container's job was admitted through the mounted socket.
   - Reservation rule: Doug, 24 September (relayed by the writer, then held): "We reserve what they ask for and then if it cost less we remember that for next time". tessera_ledger_wants is still in the working tree, waiting on his clarification. Not reverted.

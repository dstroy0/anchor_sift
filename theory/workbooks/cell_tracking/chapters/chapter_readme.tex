% Generated by maint/texbuild/theory_tex.py from theory/workbooks/cell_tracking/README.md.
% Edits here are overwritten. Edit the markdown, then run the script again.
\chapter{\texorpdfstring{The workbook is the ledger}{The workbook is the ledger}}
\label{chap:readme}

\textbf{Purpose:} One place where every idea the cell program rests on is written down beside what stands behind it, so a later session knows at a glance which claims are proved, which are measured, which are built but unmeasured, and which are still only theory.

\textbf{Scope:} \texttt{workbooks/\allowbreak{}cell\_\allowbreak{}tracking/\allowbreak{}}. The thought experiment that sets the sieve on the cell program is in \texttt{thought\_\allowbreak{}experiments/\allowbreak{}cell\_\allowbreak{}tracking/\allowbreak{}}. The machine the program runs on is the engine workbook, \texttt{workbooks/\allowbreak{}engine/\allowbreak{}}.

\section{\texorpdfstring{How an entry is kept}{How an entry is kept}}

Every claim carries one status, and the status says what backs it:

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.3317\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.6683\linewidth-2\tabcolsep\relax}}
\toprule
status & what it means \\
\midrule
\endhead
\textbf{proved} & an exact check ran over the whole set and found no exception; the count is given \\
\textbf{measured} & a number was taken on named samples; the number is given, and whether it held up \\
\textbf{built} & the code exists and runs; no measurement yet says whether it helps \\
\textbf{theory} & stated, not built \\
\textbf{refuted} & measured, and the measurement went against it; kept, with the number, so it is not tried again blind \\
\textbf{not so} & fails on its own arithmetic or in any machine, before anything is measured; kept, with the reason \\
\bottomrule
\end{longtable}
}

A claim changes status only when a run changes it, and the run is named. Nothing is deleted from the ledger. An idea that failed stays, with the number that failed it.

\section{\texorpdfstring{Entries}{Entries}}

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.4392\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.5608\linewidth-2\tabcolsep\relax}}
\toprule
file & what it holds \\
\midrule
\endhead
\hyperref[chap:cell-tracking-table]{cell\_\allowbreak{}tracking\_\allowbreak{}table.md} & the cell program's n-body problem part by part: the physics, what the program does for each part, what it wants, every hypothesis tried with its result, the driver's stages, and the tracker's equation as reads of the machine \\
\hyperref[chap:on-the-engine]{on\_\allowbreak{}the\_\allowbreak{}engine.md} & what the engine's books measured on the cell program's samples, and the sieve's sections that set it on the cells \\
\hyperref[chap:build-plan]{build\_\allowbreak{}plan.md} & the order the cell program's wants are built in, and every ruling on them, dated \\
\hyperref[chap:ledger]{ledger.md} & every measurement of the tracker and its driver, dated, in the order it was taken, with its samples and its result \\
\hyperref[chap:scan-then-sort]{scan\_\allowbreak{}then\_\allowbreak{}sort.md} & the rule that orders the driver: every frame of every sample scanned first, sorting only at the end \\
\hyperref[chap:walk-back]{walk\_\allowbreak{}back.md} & the rule every tracker stage is held to: it can be walked back to what it came from \\
\hyperref[chap:build-right-first]{build\_\allowbreak{}right\_\allowbreak{}first.md} & the rule against building an interim version already known to need redoing \\
\hyperref[chap:records]{records.md} & the commit texts written for the cell program, dated, as they were written \\
\bottomrule
\end{longtable}
}

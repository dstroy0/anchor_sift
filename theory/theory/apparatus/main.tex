% The apparatus. Hand-written, transcribed from docs/ on 2026-09-11.
%
% Compile from this directory so the shared inputs resolve:
%     latexmk -xelatex -outdir=../../../build/theory/theory/apparatus main.tex
%
% WHAT HOLDS THESE THREE TOGETHER
%
% None of them is a measurement of SHA-256. They are the machinery the measurements are taken with:
% a proof that a transform's constants have the order they are supposed to have, a single unit every
% reading in the tree can be quoted in, and a survey of the viewer that draws the results. Chapters
% about apparatus, and not about the object.
%
% All HELD. The twiddle work is the arbitrary-precision engine, which the partition rule names
% directly, and the other two carry SHA-256 throughout as their worked example.
\documentclass[11pt,paper=a4,twoside,openright,titlepage,
headinclude,footinclude,BCOR=5mm]{scrbook}

\input{preamble.tex}

\begin{document}
\frenchspacing
\emergencystretch=3em
\hbadness=10000
\vbadness=10000
\hfuzz=8pt
\cfoot*{}
\ofoot*{\pagemark}

\frontmatter
\pagestyle{plain}
\include{frontmatter/titlepage}

\tableofcontents

\mainmatter
\pagestyle{scrheadings}

% chapter_twiddle_proof IS NOT IN THIS BOOK and that is deliberate. docs/twiddle-proof.md is
% classified EXTERNAL, cleared for publication under "precision fair", so it was transcribed into
% theory/theory/precision/ instead. Binding it here would have put cleared material inside a held book,
% which costs nothing in safety and loses the only book that can currently be published.
\include{chapters/chapter_information_theory}
\include{chapters/chapter_viewer_survey}
\end{document}

% !TEX TS-program = lualatex
% Hand-written. markdown_to_latex.py is retired: theory/ is the source.
%
% The line above picks the engine in TeXworks and TeXShop. Typesetting this with pdfLaTeX stops on
% \RequireLuaTeX in the preamble, and preamble.tex on its own stops sooner than that: it carries no
% \documentclass because this file supplies one and then reads it.
%
% Compile from this directory so the shared inputs resolve:
%     latexmk -pdf -outdir=../../build/theory/crystallography main.tex
\documentclass[11pt,paper=a4,twoside,openright,titlepage,
headinclude,footinclude,BCOR=5mm]{scrbook}

\input{../preamble.tex}

\begin{document}
\frenchspacing
\emergencystretch=3em
\hbadness=10000
\vbadness=10000
\hfuzz=8pt
\cfoot*{}
\ofoot*{\pagemark}

\frontmatter
\pagestyle{plain}
\include{frontmatter/titlepage}
\input{../dedication.tex}
\include{frontmatter/abstract}

\tableofcontents

\mainmatter
\pagestyle{scrheadings}

\include{chapters/chapter_whose_result}
\include{chapters/chapter_the_measurement}
\include{chapters/chapter_evidence}
\include{chapters/chapter_quantization}
\include{chapters/chapter_where_it_stops}
\end{document}

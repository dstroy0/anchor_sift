\pdfbookmark[1]{Abstract}{Abstract}
\chapter*{Abstract}

A crystal is the one control in this work whose answer was written down by somebody else before the
instrument existed.

Tile a published unit cell, carry every site as an exact integer, and score how well the arrangement
agrees with itself shifted along one axis. The offset where agreement peaks is the cell edge. Across
453 axes drawn from the Crystallography Open Database, every recovered period equals the published
edge as an integer, with no tolerance applied and no error left to average.

That exactness is recent and the chapter keeps what it replaced. Until 2026-09-09 the sites were
assigned to voxels of 0.25 angstroms and the tiling was capped at 320 voxels along the longest side.
Both bounds came from this work and neither came from the deposit, and they cost a mean absolute
error of 0.0124 angstroms together with a bias of $-0.0067$ that the reading could not account for.
The cap also refused large cells outright, which never appeared as error because those entries went
unread. Carrying the sites as exact integers removes the sample spacing, the straddling lags, the
fraction to resolve and the error distribution at once.

One correction here is not about the reader. A lattice of period $P$ agrees with itself as well at
$2P$ and at $3P$, so taking the tallest lag reports a harmonic. Read that way, published edges came
back at almost exactly twice their value on ten of eighteen axes. Scoring a candidate together with
its multiples fixes it, and that rule survives the removal of the grid unchanged, because it repairs
a property of periodic sets and not damage the reader did.

What this does not license is stated with it. The reading recovers the number the deposit carries
and stops there, only right-angled cells are used, and exact agreement does not make the
published edge true. The interest is not the cell edge, which is published to more decimal places
than anything here reaches. The interest is that a memoryless control can only show an instrument
does not invent structure, and a crystal lets it show that it finds structure that is there.
\cleardoublepage

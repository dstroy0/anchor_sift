% !TEX TS-program = lualatex
% Edited by hand. Nothing generates this file; sh maint/texbuild/build_theory.sh compiles it.
%
% The line above picks the engine in TeXworks and TeXShop. Typesetting this with pdfLaTeX stops on
% \RequireLuaTeX in the preamble, and preamble.tex on its own stops sooner than that: it carries no
% \documentclass because this file supplies one and then reads it.
%
% Compile from this directory so the shared inputs resolve:
%     latexmk -pdf -outdir=../../build/theory/image_transforms main.tex
\documentclass[11pt,paper=a4,twoside,openright,titlepage,
headinclude,footinclude,BCOR=5mm]{scrbook}

\input{preamble.tex}

\begin{document}
\frenchspacing
\emergencystretch=1em
\hbadness=2000
\vbadness=10000
\hfuzz=2pt
\cfoot*{}
\ofoot*{\pagemark}

\frontmatter
\pagestyle{plain}
\include{frontmatter/titlepage}
\input{dedication.tex}
\include{frontmatter/abstract}

\tableofcontents

\mainmatter
\pagestyle{scrheadings}

\include{chapters/chapter_observed_motion}
\include{chapters/chapter_translation}
\include{chapters/chapter_rotation_scale_perspective}
\include{chapters/chapter_waves_on_a_surface}
\include{chapters/chapter_exact_arithmetic}
\include{chapters/chapter_sources}
\end{document}

% !TEX TS-program = lualatex
% Edited by hand. theory/ is the source; docs/research points here.
% Build with sh maint/texbuild/build_theory.sh anchor_sift
%
% The line above picks the engine in TeXworks and TeXShop. Typesetting this with pdfLaTeX stops on
% \RequireLuaTeX in the preamble, and preamble.tex on its own stops sooner than that: it carries no
% \documentclass because this file supplies one and then reads it.
\documentclass[11pt,paper=a4,twoside,openright,titlepage,
headinclude,footinclude,BCOR=5mm]{scrbook}

\input{preamble.tex}

\begin{document}
\frenchspacing
\emergencystretch=1em
\hbadness=2000
\vbadness=10000
\hfuzz=2pt
\cfoot*{}
% No \ofoot here. classicthesis puts the folio in the running head and gives the footer a folio on
% plain pages only. An \ofoot*{\pagemark} sets both styles and printed the page number twice on
% every page of the mainmatter, once in the header and once at the foot.

\frontmatter
\pagestyle{plain}
\include{frontmatter/titlepage}
\input{dedication.tex}
\include{frontmatter/abstract}

\tableofcontents

\mainmatter
\pagestyle{scrheadings}

\include{chapters/chapter_delta_null}
\include{chapters/chapter_delta_null_method}
\include{chapters/chapter_delta_null_missing_term}
\include{chapters/chapter_terms}
\end{document}

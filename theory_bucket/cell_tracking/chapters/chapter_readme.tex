% Generated by maint/texbuild/theory_tex.py from theory/workbook/README.md.
% Edits here are overwritten. Edit the markdown, then run the script again.
\chapter{\texorpdfstring{The workbook is the ledger}{The workbook is the ledger}}
\label{chap:readme}

\textbf{Purpose:} One place where every idea the engine rests on is written down beside what stands behind it; a later session knows at a glance which claims are proved, which are measured, which are built but unmeasured, and which are still only theory.

\textbf{Scope:} \texttt{theory/\allowbreak{}}. The thought experiments are in \texttt{thought\_\allowbreak{}experiments/\allowbreak{}}: most can be tested and some cannot yet, and each is kept as it was written, with only Gemini's export artifacts removed (the \texttt{[cite: …]} markers, which pointed at nothing, and a medical disclaimer Gemini attached after misreading exact limb arithmetic).

\section{\texorpdfstring{How an entry is kept}{How an entry is kept}}

Every claim carries one status, and the status says what backs it:

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.3317\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.6683\linewidth-2\tabcolsep\relax}}
\toprule
status & what it means \\
\midrule
\endhead
\textbf{proved} & an exact check ran over the whole set and found no exception; the count is given \\
\textbf{measured} & a number was taken on named samples; the number is given, and whether it held up \\
\textbf{built} & the code exists and runs; no measurement yet says whether it helps \\
\textbf{theory} & stated, not built \\
\textbf{refuted} & measured, and the measurement went against it; kept, with the number; it is not tried again blind \\
\textbf{not so} & fails on its own arithmetic or in any machine, before anything is measured; kept, with the reason \\
\bottomrule
\end{longtable}
}

A claim changes status only when a run changes it, and the run is named. Nothing is deleted from the ledger. An idea that failed stays, with the number that failed it.

\section{\texorpdfstring{Entries}{Entries}}

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.3830\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.6170\linewidth-2\tabcolsep\relax}}
\toprule
file & what it holds \\
\midrule
\endhead
\hyperref[chap:keys-explained]{keys\_\allowbreak{}explained.md} & the ideas a programmer's defaults push against, from first principles with the engine's numbers: transitivity carried all the way, the imprint, AND and add, unbounded operations in a small key, folding a check into a pass, exact arithmetic with no floor, the noise read and never modeled \\
\hyperref[chap:imprint-key-cycle]{imprint\_\allowbreak{}key\_\allowbreak{}cycle.md} & the atom; imprinting a program onto the impulse; keys as AND masks; running a key over the set in one cycle \\
\hyperref[chap:noise-sieve-tower]{noise\_\allowbreak{}sieve\_\allowbreak{}tower.md} & the transfinite noise sieve and the fluidic architecture, section by section against the engine: control and data planes, the key and LUT, the tower and floor −4, the demon's eyes and arms, the construct kit, identity as coherence, entropy \\
\hyperref[chap:engine-table]{engine\_\allowbreak{}table.md} & the machine part by part, held to no scale: its exact algebra, what each part does and wants, every hypothesis tried with its result, and the audit of every number that still fixes a scale \\
\hyperref[chap:two-crystals]{two\_\allowbreak{}crystals.md} & the record machine over the 2-adic integers: the wrap as a projection, the five operations that commute with every projection and the test that proves it, the exact quotient by an odd divisor as a 2-adic product, the bits the lifting reads, the two limits of the finite windows and the solenoid between them, what passes to a limit, and Doug's posits bounded \\
\hyperref[chap:kolmogorov-arnold]{kolmogorov\_\allowbreak{}arnold.md} & the Kolmogorov–Arnold representation theorem held exactly: where the engine already has its shape (the tower's lifting, the residual, the binomial ladder as integer B-splines, the cycle's sums), why that is not a KAN (a KAN fits float splines and rounds; the engine fits nothing and rounds nothing), and what finishes it \\
\hyperref[chap:compression-table]{compression\_\allowbreak{}table.md} & the crystal's size set by set against the floor the data allows: the ladder of exact bounds from raw to the noise floor, every coder variant tried, and the order to close the gap \\
\hyperref[chap:tessera-scheduler]{tessera\_\allowbreak{}scheduler.md} & the scheduler: how jobs from separate processes share one device by memory, the accounting and its invariant, measuring by pid on Linux and Windows, one daemon per host \\
\hyperref[chap:obsignatio-seal]{obsignatio\_\allowbreak{}seal.md} & the seal: the dimensional Merkle DAG of keyed BLAKE3 nodes over every crystal and set, what it proves, what it costs, and where it stops \\
\hyperref[chap:cell-tracking-table]{cell\_\allowbreak{}tracking\_\allowbreak{}table.md} & the cell program's n-body problem part by part: the physics, what the program does for each part, what it wants, every hypothesis tried with its result, and the tracker's equation as reads of the machine \\
\hyperref[chap:ledger]{ledger.md} & every measurement, dated, in the order it was taken, with its samples and its result \\
\bottomrule
\end{longtable}
}

\section{\texorpdfstring{Thought experiments}{Thought experiments}}

{\small
\setlength{\tabcolsep}{6pt}
\begin{longtable}{>{\RaggedRight\arraybackslash}p{\dimexpr0.4288\linewidth-2\tabcolsep\relax}>{\RaggedRight\arraybackslash}p{\dimexpr0.5712\linewidth-2\tabcolsep\relax}}
\toprule
file & origin \\
\midrule
\endhead
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}noise\_\allowbreak{}sieve\_\allowbreak{}1\_\allowbreak{}rule\_\allowbreak{}propagation.\allowbreak{}md} & the first draft: top-down rule propagation down the tower to floor −4 \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}noise\_\allowbreak{}sieve\_\allowbreak{}2\_\allowbreak{}lut\_\allowbreak{}beamformer\_\allowbreak{}collapse.\allowbreak{}md} & adds the binary LUT engine, two's complement exact division, the beamformer and the 2D collapse \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}noise\_\allowbreak{}sieve\_\allowbreak{}3\_\allowbreak{}tower\_\allowbreak{}scheduling\_\allowbreak{}floor\_\allowbreak{}minus\_\allowbreak{}4.\allowbreak{}md} & adds temporal stacking, the master schedule and floor −4 as the irreducible limit \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}noise\_\allowbreak{}sieve\_\allowbreak{}4\_\allowbreak{}laplace\_\allowbreak{}demon.\allowbreak{}md} & adds the demon: shift agreement as its eyes, the identity:null permutation as its arms \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}noise\_\allowbreak{}sieve\_\allowbreak{}5\_\allowbreak{}cell\_\allowbreak{}tracking\_\allowbreak{}harmonics.\allowbreak{}pdf} & the sieve applied to this competition: the engine holds nothing and gradients define themselves; a split as a boundary discontinuity; mitosis and lysis judged over the whole sample by an entropy bump; each body's spherical harmonic fingerprint, dipole and quadrupole dominant \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}fluidic\_\allowbreak{}1\_\allowbreak{}demon\_\allowbreak{}uroboros\_\allowbreak{}construct\_\allowbreak{}kit.\allowbreak{}md} & the fluidic tower: the demon observer, the self-feeding loop, the three irreducible sets \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}fluidic\_\allowbreak{}2\_\allowbreak{}elevator\_\allowbreak{}identity\_\allowbreak{}entropy.\allowbreak{}md} & the elevator operator, recursive inflation, identity as the limit of coherence, binary probes, entropy \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}subtractive\_\allowbreak{}cosmological\_\allowbreak{}framework.\allowbreak{}pdf} & a conversation, eight pages: rules propagating at field speed, not instantly; arrival as a wavefront; listening at the noise floor; phase cancellation; the identity:null permutation as field noise readings only \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}cyclic\_\allowbreak{}field\_\allowbreak{}inversion\_\allowbreak{}seed\_\allowbreak{}crystal.\allowbreak{}pdf} & exported as “Solving N-Body Problems Deterministically”, though it holds no n-body method: a conversation, four pages, on the field inverting at maximum entropy, the seed crystal, and the needle snap just off zero radius that breaks symmetry and sets the next field's propagation speed \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}hash\_\allowbreak{}boundary\_\allowbreak{}functional\_\allowbreak{}folding.\allowbreak{}md} & the boundary functional of a 2\textasciicircum{}120 hash keyspace as a threshold latch, and folding schemes that keep an accumulator one size however much is folded \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}demon\_\allowbreak{}utm\_\allowbreak{}four\_\allowbreak{}noise\_\allowbreak{}vectors.\allowbreak{}md} & the demon and a non-halting UTM treating video noise as deterministic: the residual tensor F − I split into four vector magnitudes (shot, thermal/read, fixed pattern, quantization) integrated over n frames \\
\texttt{thought\_\allowbreak{}experiments/\allowbreak{}utm\_\allowbreak{}demon\_\allowbreak{}openqasm.\allowbreak{}md} & a UTM paired with Laplace's demon running an OpenQASM circuit: the 2\textasciicircum{}n state vector, unitaries, and measurement without dice \\
\bottomrule
\end{longtable}
}

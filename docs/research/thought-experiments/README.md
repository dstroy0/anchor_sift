# Thought experiments, sorted by how wacky and whether anything can refuse them

**Purpose:** Say which of these ideas a bench could kill, separating the ones that carry weight from the ones that are the fun end.
**Scope:** `docs/research/thought-experiments/`, `docs/research/delta-null.md`, `docs/research/anchor-sift-missing-term.md`

Everything here is speculation and is kept apart from the ledger for that reason. The ledger holds claims with a measurement beside them. These hold claims without one.

Two axes matter and they are not the same axis. An idea can be very strange and still name something a bench can refuse, and an idea can be modest and name nothing at all. Sorting only by strangeness would file the useful ones with the ornamental ones.

| idea | wacky | can a bench refuse it | where it lives |
|---|---|---|---|
| Ordering anchors by symbol rarity | no | yes, and it has not been run | `anchor-sift-missing-term.md`, in the Anchor Sift theory |
| Delta null as a topological boundary | yes | yes, and two of three claims are already answered | here, `delta-null.md` |
| Absolute zero as the maximum entropy background | yes | no, not by anything in this repository | here |
| Suppositions, by the hour | mixed, item by item | some of them | here |
| Unification and the continuum | yes | no | here |

Delta null sits here with the rest and should be read first, because it is the case where being strange and being checkable are not in tension. Two of its three claims are already measured and one of those is already refused.

## The one that is not a thought experiment

`anchor-sift-missing-term.md` was written down beside these and does not belong with them. It proposes ordering the anchor set by the rarity of the symbols each anchor tests, so the rarest is checked first and the first comparison carries the most rejection power. That is a concrete change to a function that exists, it predicts a specific direction of movement in a number the bench already prints, and it costs one sort at plan time.

It is filed into the Anchor Sift theory and it is unmeasured. On the skewed corpus the first probe currently refutes about 67 percent of alignments, the figure a collision probability of 0.33 gives. The claim is that ordering by rarity raises that materially. Nothing here has run it.

## Suppositions, by the hour

That file is a notebook and its sixteen entries do not share a category. Several are concrete enough to act on:

- Crystallography, doping as a cryptographic salt, and predicting legal doping arrangements from measured distributions. Names a real dataset and a real prediction.
- Proteins as vectors and magnitudes preserving assembly order. Partly done: `representation/structure/protein.py` reads a backbone that way, and the bond lengths come back at 1.45, 1.52 and 1.33 against chemistry's 1.46, 1.52 and 1.33.
- Tail error under a periodic disturbance, and the 10^-26 target.

Others are cosmology and belong exactly where they are. The file is kept whole and not split into piles, because the numbering is the order the thinking happened in and cutting it up would lose that.

## What keeps this honest

Nothing in this directory may be cited as a result. If one of these is measured, the measurement goes in the ledger with its number and its conditions, and the entry here says which ledger row answered it.

**Author:** dstroy0 (Douglas Quigg) <dquigg123@gmail.com>
**Date:** 2026-09-08

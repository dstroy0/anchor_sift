#!/usr/bin/env python3
# anchor_sift - Copyright (C) 2026 Douglas Quigg (dstroy0) <dquigg123@gmail.com>
# SPDX-License-Identifier: AGPL-3.0-or-later OR LicenseRef-Commercial OR LicenseRef-Educational
"""Agreement with an answer that came from outside the sample.

A negative control shows only that an instrument does not invent structure. An oracle is the other
kind: a published cell edge, a dialect border somebody else labeled, a bond length fixed by valence.
The answer exists before the measurement and is not supplied by it. Most readings in this work have
lacked that condition.

Supervision is also the only one of the three ways a partition is fixed that adds information the
sample did not hold, so this part is where new information enters the engine and nowhere else.

What belongs here: two direction checks against a source, corpus gates, and positive controls with
published ground truth. What does not: a null permutation, which is a reference built from the
object itself and settles nothing from outside it.
"""
